# 更新日志

[English](CHANGELOG.md) | **简体中文**

本文件记录项目的重要变更，并与英文 `CHANGELOG.md` 在同一次提交中维护。

## [Unreleased]

## [0.3.0] - 2026-09-07

- **破坏性变更：** 将公开的 `/rbac` 路由和非 GET/POST 管理方法替换为中性的
  `/api/v1/permissions`、`/api/v1/roles`、`/api/v1/users` 和
  `/api/v1/system` 资源及动作路由；公开 OpenAPI 元数据和错误码不再暴露内部授权
  机制。
- 增加至少十三个权限、角色生命周期、角色权限绑定和用户角色管理接口，并要求严格
  权限校验，以及在共享角色变更时使用强 ETag/`If-Match` 并发前置条件。
- 增加不可变的 `super_admin`、`admin`、`user` 系统角色、新用户事务内强制绑定
  `user`、自定义角色软删除、受限管理员权限，以及首位超级管理员的离线原子引导
  流程。
- 增加旧版多持有人升级拦截、`super_admin` 绑定变更后恰好一位持有人的数据库约束，
  并永久保留历史角色键 `owner`；角色写响应正文与 ETag 现在来自同一锁内快照，
  路由层拒绝的高权限写操作也会记录审计。

## [0.2.0] - 2026-09-06

- **破坏性变更：** 将授权模型重构为单项目 RBAC 基线，使用用户、用户角色关联和
  唯一的全局授权状态；访问令牌只携带最小身份字段和协议字段。本次变更替换旧版
  表结构、API 路由、JWT 和锁协议，不能从 `v0.1.0` 直接覆盖升级。
- 增加按需读取的代理可用性检测参考，涵盖强制通过指定代理发送请求、安全错误映射、
  Redis 最近结果缓存、列表回显、固定五个 worker 的前端批量检测和受控路由测试。
- 增加仓库文档的简体中文对应版本，并为 GitHub Issue 与 Pull Request 提供双语
  提示。

## [0.1.0] - 2026-09-05

- 完成独立 Skill 的首次公开预览发布准备。
- 增加 XTN 所有权说明、上游署名、第三方声明、仓库治理、安全报告和自动验证。
- 随附的 PostgreSQL RBAC 资产会拒绝空 JWT 密钥、公开示例占位符，以及短于
  32 字节的 JWT 密钥。
