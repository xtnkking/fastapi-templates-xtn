# 更新日志

[English](CHANGELOG.md) | **简体中文**

本文件记录项目的重要变更，并与英文 `CHANGELOG.md` 在同一次提交中维护。

## [Unreleased]

## [0.6.0] - 2026-09-20

- 增加请求级 API 多语言：全部面向客户端的运行时响应文案和安全参数校验明细提供
  完整 `zh-CN`、`en` 资源，默认中文，通过有界的标准 `Accept-Language` 协商，
  并返回规范化 `Content-Language` 与 `Vary: Accept-Language`。HTTP／业务码、响应
  数据、请求 ID、日志、审计字段和内部原因码保持稳定且不翻译。更具体的 `q=0`
  排除规则优先于父级范围和通配符；安全的 `422` 字段提示不会回显额外字段或深层
  字典键；未捕获的 `500` 也会保留两项语言响应头。

- **破坏性变更：** 管理员重置密码改为项目级选择。默认 `direct`：管理员提供的
  新密码立即成为用户正式密码；可选 `temporary`：登录签发 Token 前，用户必须先
  完成一次正式密码设置。生成项目时必须解释并询问两种模式，接口调用者不能按单次
  请求切换。`AdminPasswordResetRequest.temporary_password` 改名为
  `new_password`。两条路径都保留图形验证码、准确权限、严格低级目标、旧 Token
  撤销、原子账号安全审计和秘密脱敏。现有 `users.must_change_password` 已足够支持
  两种模式，不增加迁移或数据表。从 `v0.5.1` 更新且明确要保留原来“重置后必须
  完成临时密码”的项目，需要显式设置 `ADMIN_PASSWORD_RESET_MODE=temporary`。

## [0.5.1] - 2026-09-19

- 修正登录和注册的执行顺序：先计算公开入口的可信 IP 业务额度，再消费图形验证码，
  最后才校验密码、创建用户或签发 Token。限流拒绝不会误用验证码；只要请求已经
  进入业务并提交错误验证码，就仍会占用一次登录或注册额度。
- 登录后的接口改用显式且不可变的限流映射；操作没有登记时直接拒绝，不再静默
  落入普通写入额度。管理员强制下级全部退出改用管理变更额度。
- 用户和角色的可见范围、精确用户名搜索、软删除过滤、分页和总数均在 PostgreSQL
  查询中完成。用户管理响应补充 `user_name`，被隐藏的平级、上级与不存在目标继续
  使用相同表现。
- 当前登录列表响应增加 `Cache-Control: no-store`；新增独立的 `/health/live` 和
  依赖感知的 `/health/ready`。就绪检查要求唯一且正确的 Alembic head、全局
  `rbac_state` 行，并要求两套 Redis 都通过 `PING` 和 Lua 写入、读取、删除随机
  五秒 Key 的探测。检查使用可配置的短超时，只返回整体结果，不泄露连接、Key、
  单个组件状态或异常细节。
- 压缩主 Skill 入口并增加按需阅读的中文架构总览，同时保留详细 reference 的按任务
  路由。将 [v0.4.0 设计讨论](docs/history/v0.4.0/DESIGN_REVIEW.zh-CN.md)移入历史
  归档，避免被误认为当前实现。
- CI 使用完整提交 SHA 固定 Action，增加 Ruff 格式检查、依赖漏洞扫描、更严格的
  发布校验和路由限流映射检查。官方验证环境为 Python 3.12、PostgreSQL 17、Redis 7。
- 针对依赖扫描发现的问题，将测试依赖固定为 `pytest>=9.0.3,<10`，并配套使用兼容的
  `pytest-asyncio>=1.4,<2`；CI 在安装和扫描依赖前先升级到 `pip>=26.2`，不通过
  临时忽略掩盖这些漏洞。
- 本版本不改变数据库表和迁移；`v0.5.0` 已发布的四个迁移文件继续保持不变。

## [0.5.0] - 2026-09-13

- **破坏性变更：** 新项目只用必填、软删除后不可复用的 `user_name`。
  公开注册默认开启，以数据库中的开关供超级管理员调整；关闭公开注册后，有权限
  的管理员仍可新增用户。两种创建路径都只授予 `user` 角色。登录、注册、管理员
  新增／重置、本人改密必须使用五种互不串用且每张只尝试一次的图形验证码；基础
  模板不捆绑邮箱／短信发送模块和忘记密码自助接口。
- **破坏性变更：** 去掉全站和跨业务 Token Bucket 额度，改为 Redis
  原子固定窗口，每个业务单独计数。未登录的登录、注册、完成临时密码及领取
  公开验证码按可信 IP 加业务计数；已登录操作（包括领取验证码）按用户 ID
  加业务计数，不按提交的用户名限流。验证码每场景、每主体默认五分钟最多
  十张。次数可调整，生成项目前要告知
  使用者默认值。
- **破坏性变更：** 移除在线移交超级管理员、普通用户退出全部登录、
  独立的 `can_delegate` 开关和接口。更换超级管理员采用有锁、有审计的线下 SQL。
  本人只退出当前登录；有权限的管理员只可强退严格下级。使用者选择同账号最多
  同时登录次数，达到上限时新登录成功后退出最早的一次；Redis 只记录登录时间，
  不辨认物理设备。授予权限仍不得超过操作者自己的有效权限。
- 国家目录与代理检测仍为按需功能，不能默认开放匿名普通业务查询或全站限流。
  发布校验与 `v0.5.0` 清单已跟进上述规则，已发布 `v0.4.0` 历史记录不改写。
- **破坏性变更：** 本地密码状态直接存入 `users`：`password_hash` 和
  `password_changed_at` 可空，`must_change_password` 非空且默认 `false`，保留
  `token_version`。删除独立的 `user_password_credentials` 表和密码凭据事件版本。
  用户软删除时清空哈希，恢复时不复活旧密码。改密次数按成功的账号安全审计事件统计，
  不新增独立的可变计数器。参考资产用于新项目，不提供从 `v0.4.0` 资产就地升级
  数据库的迁移脚本。
- 修复格式错误的验证码请求绕过拒绝额度、刷新时生成图片失败导致旧验证码失效、
  以及 PostgreSQL 非空数据库回滚迁移时的顺序问题。新增资产配置说明，完成
  PostgreSQL 17 与 Redis 的真实环境测试。

## [0.4.0] - 2026-09-13

- **破坏性变更：** 绿地服务必须在用户建模前先确定 `users` 存储 `email`、
  `user_name` 或两者，再分别确定各创建流程的必填规则、登录输入、规范化、唯一性、
  跨字段歧义和删除后复用；现有服务默认保留既有契约。JWT `sub` 和离线首位超级
  管理员引导现在只使用不可变的 `users.id` 标识用户，引导参数为
  `super_admin_user_id`。
- **破坏性变更：** Access Token 默认声明现在严格只有 `sub`、`jti`、`iat`、`exp`
  和 `token_type`。`iss` 与 `aud` 是成对的可选加强项：为新项目启用前，Skill 必须
  先用大白话说明它们能减少 Token 被错误服务接受的风险，但会增加签发方和验证方的
  配置协调，并取得使用者明确同意；未回复不算同意。存量项目已经同时配置二者时默认
  保留。Redis 活跃 JTI 命名空间不再依赖可选的公开 issuer 声明。
- 增加集中配置的两层 HTTP 限流，并且一次事件涉及的全部限流桶由 Redis 原子判断。
  可运行资产覆盖最外层系统总量/可信 IP、认证后的读写分类、权限管理写入、
  `super_admin` 移交和全部退出。真正超限返回 HTTP `429`、业务码 `429001` 与
  `Retry-After`；Redis 无法可靠判断或状态损坏时关闭放行并返回 `503001`。Redis Key
  使用用途隔离的 HMAC，不暴露原始 IP、账号、用户 ID、Token 或 JTI。CI 和本地开发
  环境使用独立于活跃 JTI 的第二个 Redis 来验证这些状态。`APP_ENVIRONMENT` 现在是
  没有代码默认值的必填项，遗漏时不能再悄悄继承 `development`。只有明确命名的本地或
  测试环境可以关闭限流，其他环境会在启动时拒绝，避免部署后的注册、登录、临时密码完成
  和当前密码复验通过配置绕过 `IdentityAbuseFlow`。
- 增加可复用的用户名密码登录与注册防滥用，以及默认关闭的可选邮箱/短信验证码
  Service。登录硬限制只有 IP、IP+规范化账号和系统总量；登录失败只会原子更新一个
  自动过期的私有风险信号，该计数不能单独拒绝请求，也不能阻止系统校验正确密码。
  CAPTCHA、邮箱/短信验证码和 MFA 只有在使用者明确选择后才加入。启用验证码时，验证码
  由 `secrets` 生成，绑定用途、渠道和目标，只以 HMAC 保存，5 分钟到期，最多输错
  5 次，重发会替换旧的活跃验证码，并且只能成功消费一次。供应商和公开登录/注册路由
  仍由具体产品通过适配器接入。生成某项控制前，Skill 必须向使用者展示所有适用的默认
  次数，并询问是接受全部默认值还是列出修改项；未回复不算同意，生产系统总量和供应商
  容量仍需结合真实部署单独确认。可选的同一目标发送策略命名为
  `verification_target_average`：初始有 5 次额度，之后按平均每 24 小时 5 次持续补充；
  它不会按日历日清零，也不代表任意连续 24 小时内绝对不超过 5 次。
- 补齐此前只有防滥用原语、没有真实认证流程的问题：增加在线程外运行的 Argon2id、
  软删除密码凭据事件、公开注册与登录、本人改密、只能针对严格低层用户的管理员重置、
  一次性临时密码完成流程，以及唯一 `super_admin` 的离线找回命令。密码轮换会递增
  `users.token_version`，并与只追加的账号安全证据原子提交。认证只使用一个短期
  Access Token，到期后重新登录，不增加逐 Token PostgreSQL 表。Skill 现在会一次性询问身份与登录字段、注册方式、经过验证
  的找回凭据、多设备登录和已有账号接入，同时把密码算法与事务安全细节保留为固定默认。
  发布校验现在会检查 Python 路由、设置、Argon2、模型、离线工具和 Alembic 结构，不再
  仅凭相关文档文件存在就认定这套基线完整。即使用户只剩软删除的历史凭据，新凭据版本
  也会从历史最大值继续递增；直接调用 Service 不能把当前密码或临时密码原样作为新密码；
  基础设施故障也不会被错误记录成账号安全拒绝事件。
- 公开自助注册现在通过 `PUBLIC_REGISTRATION_ENABLED=false` 默认关闭：关闭时不会注册
  路由，也不会出现在 OpenAPI 中；明确启用后仍使用受保护的 `POST` 流程并只绑定最低
  `user` 角色。
- **破坏性变更：** 每个用户最多只能有 10 个未解绑角色，其中必需的 `user` 和可能
  持有的 `super_admin` 都计数；已禁用角色仍计数，软删除历史不计数。服务层会在全局
  锁内校验完整最终集合，首次超级管理员引导和超级管理员移交遵守同一规则，PostgreSQL
  也会串行化并拒绝直接 SQL 或并发造成的超限写入。全新基线现在直接在 `0001` 安装
  该数据库防线，删除不再需要的后续兼容迁移，并以 `0004_password_auth` 作为唯一
  migration head。
- **破坏性变更：** 普通管理员现在只能列出或查看有效权限严格低于自己的未删除用户和
  角色，`super_admin` 可以读取全部未删除条目；被隐藏详情与资源不存在无法区分。
  角色和用户列表改用批量查询装配，SELECT 数量不会随页面大小增长。用户响应把
  `assigned_role_ids`、`effective_role_ids` 和生效权限明确分开：禁用角色仍保持分配并
  计入上限，但不产生当前权限。管理写操作现在先校验能力，缺少能力返回 `403001`；
  能力通过后，自己、同级、更高级、受保护、未知目标，以及绑定请求中的隐藏角色 ID
  统一返回 `404001`。只有目标可见但不满足委派或影响范围策略时才返回 `403001`。
  内部绑定/解绑 Service 入口会在打开事务前拒绝未知操作值，不再把拼写错误当成另一种
  操作执行。
- **破坏性变更：** 认证启动现在会拒绝公开示例占位值和明显薄弱的 JWT 密钥。传入的
  Bearer Token 与生成的 Access Token 均限制为 4096 个 UTF-8 字节。新增
  `POST /api/v1/auth/logout-all`，通过递增 `users.token_version`，让该账户此前签发的
  全部 Token 在后续认证时失败；该操作不会批量删除相应的 Redis 记录。
- **破坏性变更：** 移除未使用的 `roles:permissions:update` 和
  `system_owner:transfer` 权限键。公开 API 响应与 OpenAPI 统一使用 `super_admin`
  术语，不再暴露 `is_super_admin` 或应用级 ownership 用语。仅实现内部使用的角色标志
  在 ORM、SQL、约束、索引和测试中统一命名为 `is_super_admin`。历史 `owner` 角色键
  不再由系统保留或解释，现在可以作为没有任何特殊权限的普通自定义角色键使用。
- **破坏性变更：** 所有允许运行时移除的可变数据统一改为软删除，包括用户、自定义
  角色、业务实体和 RBAC 解绑。RBAC 关联采用非顺序 ID 的关系事件和仅活跃逻辑对
  唯一约束；解绑为活跃事件写入
  墓碑，重新绑定创建新事件，父对象删除在同一事务中软删除其活跃关联，恢复不会
  重新激活旧权限或 Redis JTI 状态。系统角色和固定权限目录不提供运行时删除命令。
  该规范适用于产品实际开放的每个生命周期操作；随附资产直接实现自定义角色删除和
  两类 RBAC 解绑，用户、权限目录和业务实体生命周期仍是明确的产品适配点。
- 明确审计记录保持只追加，不提供软删除路径，并拒绝运行时更新、删除和清空。
  `rbac_state(scope='global')` 单例永不删除或清空。物理清理仅限另行受控且符合条件
  的非审计墓碑；破坏性审计保留策略必须获得独立的产品和法律批准。
- 拒绝审计写入本身失败时，继续保留原始 HTTP `403`、`404` 或 `409` 授权结果，
  同时仍记录审计存储故障。
- 将 PostgreSQL 迁移角色与运行时角色分离改为初始化时询问的可选生产加固决定。
  有专业运维支持时建议分离；不得因此阻断小型或学习项目。PostgreSQL owner 或
  superuser 直接执行 SQL 可以绕过应用软删除，不属于应用能够保证的范围。
- 增加按需读取的国家/地区目录规范和 PostgreSQL 实现指南：使用 alpha-2 国家码
  作为自然主键，将可共享的国际区号保存为字符串，区分停用与软删除，使用不会根据
  缺项推断删除、也不会暗中恢复数据的原子导入，并默认只开放有效数据读取接口。
  同时增加支持精确核对产品认可国家码集合的只读 UTF-8 CSV 校验器及聚焦测试；
  正式导入只要存在非空 `flag_url`，就必须显式提供经批准的 ASCII DNS 主机名白名单；
  `--structure-only` 仍是非权威结构检查并报告 `membership_checked=false`。发布校验会
  拒绝捆绑的 CSV/TSV 国家数据和疑似国家/旗帜媒体资产；
  在来源和再分发权限未记录前，不会把第三方国家数据集发布到仓库。
- 增加相互独立且按需读取的业务审计模块和可复用 PostgreSQL 资产。它提供逐动作事件
  目录和状态字段白名单、`succeeded`/`failed`/`denied` 结果语义、数据库只追加
  约束、由调用方事务保证的成功写入、回滚后安全记录失败事件、访问及保留指南，
  并提供对应的单元测试与 PostgreSQL 测试；不会扩大 `rbac_audit_events` 的用途。
  资产还会拒绝可识别的邮箱和网络标识值，支持由外层事务持久提交的 savepoint
  证据，并阻止直接更新、删除和清空审计表。运维指南覆盖读取、导出、保留、分区、
  法律保留、告警、备份和恢复。由于外部审计投递不属于本 Skill 的基线，已删除
  原计划的 `business_audit_delivery_outbox` 表及其 Worker、租约、重试和死信设计。
- 增加按需加载的运行日志和持久审计模块。可运行资产现在会输出带请求 ID 关联和
  防御性脱敏的安全单行 JSON 请求事件，校验有界审计快照，记录可信来源与 Schema
  版本，并通过 PostgreSQL 约束及更新/删除触发器保证 RBAC 审计记录只追加。
- **破坏性变更：** 增加统一的四字段 JSON 响应：使用真实 HTTP 状态码、六位数字
  业务码、正文与响应头中相同且由服务端生成的必填请求 ID，以及仅包含
  `items`、`page`、`page_size`、`total` 的简单分页。可运行资产已将其用于成功、
  领域错误、参数校验、框架错误和未预期异常。
- **破坏性变更：** 将角色的强 ETag/`If-Match` 改为只接受严格 JSON 整数的请求体
  `expected_version`，并在获取权威锁、完成全部授权判断后才与 `roles.version`
  比较。已获授权的陈旧写入返回 HTTP `409` 和业务码 `409002`，同时避免版本探测，
  以及对每次都会改变 `request_id` 的响应正文错误使用稳定强 ETag。
- 将 RBAC 全局锁状态和管理审计相关的表、ORM 模型、约束及索引从容易混淆的授权
  命名统一调整为 `rbac_state`、`RbacState`、`rbac_audit_events` 与
  `RbacAuditEvent`。由于 Skill 目前尚无使用者，初始迁移会直接使用最终名称，
  不增加兼容迁移。
- 将首位超级管理员的 Python 引导入口替换为随附的 PostgreSQL 事务脚本。目标账户
  必须已经拥有 `user`；由使用者本人对目标数据库执行带全局锁和审计的绑定脚本，
  脚本不会创建身份，也不会暗中将首位注册者提升为超级管理员。
- UUIDv4 由统一强制要求调整为受支持的标识符方案。没有既有策略的新项目优先使用
  已登记的业务前缀和由 CSPRNG 生成的随机大写后缀，并按命名空间生命周期体量选择
  长度；已发布的 PostgreSQL 资产继续保留兼容的 UUIDv4 方案。
- 将认证指南统一为一种最小化 Access Token，默认有效期为可配置的 3600 秒；实现者
  必须告知使用者结合业务风险和登录体验重新评估有效期。
- 将逐 Token 撤销简化为必经的 Redis 活跃 JTI 门禁，随后复用现有 PostgreSQL
  用户状态、用户版本和 RBAC 查询；目标方案不增加逐 Token 数据库记录或逐请求
  Token 记录查询。
- 要求只有 Redis 注册成功后才能返回 Token，Redis 值精确绑定
  `sub`、类型、`iat`、`exp` 和用户版本；同时要求故障时关闭访问并确认精确删除后
  才返回注销成功。文档明确了在途请求与 Redis 故障转移的剩余边界，避免夸大撤销保证。

## [0.3.0] - 2026-09-07

- **破坏性变更：** 将公开的 `/rbac` 路由和非 GET/POST 管理方法替换为中性的
  `/api/v1/permissions`、`/api/v1/roles`、`/api/v1/users` 和
  `/api/v1/system` 资源及动作路由；公开 OpenAPI 元数据和错误码不再暴露内部授权
  机制。
- 增加至少十三个权限、角色生命周期、角色权限绑定和用户角色管理接口，并要求严格
  权限校验，以及在共享角色变更时使用强 ETag/`If-Match` 并发前置条件。
- 增加不可变的 `super_admin`、`admin`、`user` 系统角色、新用户事务内强制绑定
  `user`、自定义角色软删除、受限管理员权限，以及首位超级管理员的离线原子引导
  流程。
- 增加旧版多持有人升级拦截、`super_admin` 绑定变更后恰好一位持有人的数据库约束，
  并永久保留历史角色键 `owner`；角色写响应正文与 ETag 现在来自同一锁内快照，
  路由层拒绝的高权限写操作也会记录审计。

## [0.2.0] - 2026-09-06

- **破坏性变更：** 将授权模型重构为单项目 RBAC 基线，使用用户、用户角色关联和
  唯一的全局授权状态；访问令牌只携带最小身份字段和协议字段。本次变更替换旧版
  表结构、API 路由、JWT 和锁协议，不能从 `v0.1.0` 直接覆盖升级。
- 增加按需读取的代理可用性检测参考，涵盖强制通过指定代理发送请求、安全错误映射、
  Redis 最近结果缓存、列表回显、固定五个 worker 的前端批量检测和受控路由测试。
- 增加仓库文档的简体中文对应版本，并为 GitHub Issue 与 Pull Request 提供双语
  提示。

## [0.1.0] - 2026-09-05

- 完成独立 Skill 的首次公开预览发布准备。
- 增加 XTN 所有权说明、上游署名、第三方声明、仓库治理、安全报告和自动验证。
- 随附的 PostgreSQL RBAC 资产会拒绝空 JWT 密钥、公开示例占位符，以及短于
  32 字节的 JWT 密钥。
