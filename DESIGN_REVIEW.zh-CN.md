# fastapi-templates-xtn 设计核对记录

这是一份产品决定记录，用来防止跨轮对话丢失上下文，不单独证明某项代码已经通过真实数据库验证或正式发布。
按业务批量核对：说明行为、这样做的原因与代价，再记录使用者的决定。
未确认的选项不得被当作默认授权。GitHub 上的 `v0.4.0` 不因本记录而改变。

当前工作树已把以下决定写入 Skill、参考文档和 PostgreSQL 示例资产；离线测试与独立 Redis 测试已运行。真实 PostgreSQL 集成测试仍需要经确认的全新隔离测试库；本机安装版和 GitHub 下载版的状态以本次交付报告为准，不能从本记录推断已同步或已发布。

## 已确认的决定

| 主题 | 决定 | 原因和边界 |
| --- | --- | --- |
| 密码存储 | 密码哈希放在 `users` 表，不为普通用户名密码单独建立凭据表。 | 减少无必要的表和状态；只保存密码哈希，不保存明文。仓库已有上一轮未提交的对应改动，不能把它说成已发布。 |
| 临时密码 | 保留 `must_change_password`、管理员重置密码及用户凭临时密码设置正式密码的流程；它不是用户自行找回密码的接口。 | 管理员重置后仍需迫使用户改为自己掌握的正式密码；不因为删除匿名找回功能而删掉这个状态。 |
| 临时密码有效期 | 不设置到期时间；在被成功使用、再次重置或账号删除前保持有效。 | 少一套到期和重新发放流程，但遗失的临时密码也可能长期被人用于完成改密；必须可信交付，并提醒用户尽快设置正式密码。正式密码也不因本决定定期过期。 |
| 新设密码的长度与字符组成 | 默认建议 8～60 个字符；生成具体项目时说明默认值并允许其使用者调整。另须询问是否分别强制包含大写字母、小写字母、数字、特殊符号；四项默认都不强制。 | 不用复杂的字符拼凑规则挡住正常注册；除长度外只保留已确认的三类简单弱密码拦截。 |
| 明确禁止的弱密码 | 拒绝整串为数字或英文字母连续递增、连续递减的密码（如 `12345678`、`abcdefgh`、`87654321`），整串重复同一个字符的密码（如 `aaaaaaaa`），以及与该账号用户名完全相同的密码。与用户名比较时不区分大小写；密码内容不做大小写转换，数据库只保存其哈希。仅包含用户名或仅含短顺序片段不因此拒绝；与用户名相同时提示“密码不能与用户名相同”。 | 这三种简单模式即使达到最低长度也容易猜；不再增加必须维护的常见密码名单，拒绝时给用户明确提示。 |
| 额外弱密码名单 | 基础模板不强制维护或查询额外的常见/泄露密码名单。 | 降低维护与误伤成本；`password` 等不命中已确认三种模式的密码可能通过，必须如实告知安全取舍，不能声称所有弱密码都被拦截。 |
| 新项目登录名 | 基础模板只使用 `user_name` 加密码；不默认提供邮箱字段、邮箱登录、邮箱验证码或邮箱找回。需要邮箱登录的项目由其使用者自行适配。 | 让基础模板保持简单。已有项目若使用邮箱，不能擅自替换其原有身份规则。邮箱登录本身不必然要求邮箱验证码；已确认的图形验证码是登录接口的独立要求。 |
| 用户名首尾空白 | 存储和登录查找时必须去掉首尾空白，不询问项目使用者。 | 避免误输空格造成两个看似相同的账号；密码本身不做同样处理。 |
| 用户名字符和长度 | 去掉首尾空白之后，必须是 3～32 位 ASCII 英文字母、数字或下划线（`[A-Za-z0-9_]{3,32}`）；其他字符一律拒绝。注册、登录、管理员新增及查找必须使用一致的规则。 | 中文、全角字母、内部空格、标点、表情均不接受；不能先把全角等字符转换成英文字母再放行。是否区分英文字母大小写另行询问具体项目使用者。 |
| 敏感用户名 | 基础模板禁止 `admin`、`administrator`、`root`、`superadmin`、`super_admin`、`sysadmin`、`system`、`support`、`user`、`test`、`guest`、`ceshi` 这 12 个完整名称；匹配时不区分大小写，`ADMIN`、`CESHI` 也拒绝，但 `admin123`、`ceshi123` 不因此拒绝。账号实际持有对应角色也不能例外。 | 避免把账号名误当成角色、系统、客服或默认测试身份；只禁止完整匹配，避免广泛拦截包含普通词的其他用户名。 |
| 保留名提示 | 尝试注册或由管理员新增保留用户名时，明确提示“该用户名不可使用，请更换”。 | 与真实重复用户名的“用户名已被占用”区分，不声称一定存在某个账号，也不公开现有账号状态。 |
| 账号删除与名字复用 | 删除用户采取软删除；删除后原用户名永久保留，不分配给另一人。 | 保留历史归属，避免同名新旧用户在审计、客服和找回流程中混淆。 |
| 自行注册 | 项目应支持普通用户自行注册，并在后台提供服务端生效的开放注册开关；仅 `super_admin` 可操作这个开关。 | 需要能随业务需要开启或关闭对外注册；不能只隐藏前端按钮。管理员逐个新增用户的权限与向所有人开放注册的权限相互独立。 |
| 注册开关初始状态 | 默认开启；后台有权限的人可以随时关闭或重新开启公开注册。关闭注册不妨碍已有用户登录或有权限的管理员新增用户。 | 安装后可以先正常注册目标账号；代价是上线后到关闭开关前，外部用户也能注册 `user`，可能抢占用户名。注册仍须受已确认的限流规则保护。 |
| 关闭注册时的反馈 | 公开注册接口继续存在，但在服务端拒绝新注册并明确提示“暂未开放注册”；有注册页面的项目隐藏注册入口或显示同样提示。 | 不让用户把关闭注册误解为接口损坏；已有用户登录、管理员逐个新增用户不受影响。 |
| 注册开关的前后端联动 | 后端在 `/api/v1/auth/registration/status` 提供公开 `GET`（返回 `registration_enabled`）和仅当前 `super_admin` 可调用的 `POST` 修改。页面进入时读当前状态，超管修改后刷新；无须推送或轮询。提交注册时后端仍按持久状态再次检查，页面状态过时则返回“暂未开放注册”。 | 前端展示不能代替后端把关；Skill 提供接口和接入契约，具体页面由使用它的项目实现。 |
| 未登录访问边界 | 所有普通业务查询、业务变更和管理接口默认必须先登录，不能提供“匿名普通查询”能力。未登录只可访问已明确约定的认证入口：图形验证码领取/刷新、登录、公开注册、临时密码完成，以及只返回 `registration_enabled` 的注册状态 `GET`；监控用健康检查不算业务接口，不能由此开放业务数据。 | 公开注册和验证码本身必须允许未登录者访问，否则无法注册或登录；注册状态只是让页面知道是否展示入口，不得顺带返回用户或内部设置。每个例外都须明确列出，新增业务接口不能因为是 `GET` 就默认公开。 |
| 注册成功后的状态 | 自行注册只创建账号并返回用户 ID，不自动登录、不签发 Token；用户需要再登录一次。 | 把账号创建和 Redis 登录凭证签发分开；多一步操作，但不会在凭证服务故障时把已创建的账号误当作注册失败。 |
| 重复用户名提示 | 注册或管理员新增时，名字已被占用统一提示“用户名已被占用”；不区分仍在使用和已软删除的账号。 | 用户知道该换一个名字，同时不公开原账号当前状态。 |
| 管理员新增用户 | 拥有相应新增用户权限的管理员也可以创建用户。 | 与自行注册并存，不能因为开放自行注册就取消管理端创建能力；新增用户权限必须由服务端检查。 |
| 敏感操作的再次确认 | 管理员新增用户、管理员重置其他用户密码时，仍需登录并通过相应权限校验，还需输入图形验证码，但不要求管理员重新输入自己的当前密码。用户修改本人密码时需要本人旧密码和图形验证码。 | 图形验证码是防自动化提交的最低标准，不能证明操作者本人，也不能等同于密码复核；已确认的登录、权限和旧密码要求不能因此取消。 |
| 登录与注册验证码 | 公开登录、公开自行注册也必须提交图形验证码，分别与各自业务绑定；没有有效验证码时，服务端不得校验密码、签发 Token 或创建用户。 | 登录和注册都是未登录场景，不能绑定已登录用户 ID；图形验证码不等于账号所有权证明，原有登录/注册限流仍需生效。 |
| 图形验证码的定位 | 登录、注册及上述三项敏感操作共五种场景必须使用图形验证码；每张图从生成起 5 分钟有效，过期后须重新获取。每张图只允许提交一次，答案正确或错误都立即删除该验证码关联的所有 Redis key；失败后须获取新图。具体项目觉得强度不足时，可自行替换为更强的验证方式。基础模板不因此增加邮箱或手机号依赖。 | 服务端须原子地校验并删除该验证码关联的 key，防止并发提交重复通过；不能确认已消费时不得执行受保护操作。验证码通过但随后登录密码错误或注册失败，该图同样已经消费，下次须重新获取。图形验证码不能证明操作者本人。每张图的 5 分钟有效期，与下文每个主体 5 分钟领取/刷新最多 10 张的限流窗口是两回事；不能把现有邮箱/短信验证码的参数套在图形验证码上。 |
| 图形验证码按业务与登录身份隔离 | 固定用途名依次是 `login`、`register`、`admin_create`、`admin_reset`、`self_change`，对应登录、注册、管理员新增用户、管理员重置他人密码、本人修改密码。前两种从公开 `POST /api/v1/auth/captcha` 领取，后三种从需登录的 `POST /api/v1/me/captcha` 领取；后端按当前接口预期用途核对，不能跨业务混用。登录后的三项操作还要绑定领取者的当前用户 ID，别人即使拿到该图也不能提交；用户 ID 由服务端认证取得，不接受客户端指定。登录、注册不能绑定用户 ID，只按业务用途隔离，每张图仍有独立的不可猜测挑战标识。登录后不额外绑定当前设备或本次登录的 JTI。 | 用途或登录用户不匹配视为本次验证失败，按单次使用规则消费提交的那张图，不误删其他验证码。同一用户在另一处登录后，只有同时拿到未使用的挑战 ID 和正确答案才能代为提交；验证码防自动化，不证明用户本人，也不靠设备指纹增强。 |
| 主动刷新验证码 | 同一个页面成功刷新时，旧图立即失效。后端原子地确认当前挑战仍有效、取消旧图、签发新图；两次几乎同时刷新同一张图时，只允许一次替换成功。登录后可按用户 ID 加业务用途定位当前图；未登录时要由页面带上旧图的挑战标识，不能假装知道两个匿名请求是否来自同一个人。 | 并发提交与刷新按 Redis 实际处理顺序决定：先提交成功就不能由后到的刷新撤回；先刷新则旧图不能再提交。页面须避免重复刷新或忽略过时响应，免得显示已失效的图。未登录者在两个独立页面分别领取新图时，若没有匿名会话标识，服务端无法识别为同一个人；本决定只保证成功刷新所指的旧图被替换，不声称匿名用户全局只有一张有效图。 |
| 所有限流的维度 | 每个受限业务或接口有自己的额度，不共享跨业务的总桶。仅已明确开放的未登录认证入口按可信来源 IP 加固定业务标识计数；有多个用途的验证码领取接口可以再加服务端白名单中的用途。已登录时仅按固定业务标识加服务端认证的用户 ID 计数；管理员操作按操作者 ID，不按目标用户 ID。登录、注册及其他未登录认证入口都不用请求体里的用户名、邮箱、目标账号或票据作限流主体。 | 业务标识用于隔离不同接口，不是所有业务共用的“业务总额度”。这样不会因有人填写别人的用户名而单独占满该账号的额度；同一公网 IP 下的匿名使用者仍会共用该认证入口的额度，多 IP 攻击也没有全站桶兜底。缺少可信 IP 时不能把所有请求放进一个 `unknown` 共享桶或信任请求自带的转发头，应拒绝受保护操作并提示配置问题。 |
| 限流实现与可调参数 | 以简单的 Redis 固定窗口为基础：每个业务与主体只有一个计数键；Lua 一次完成 `INCR`、首次 `EXPIRE` 和读取剩余时间。首次请求开启窗口，到期后整份额度一次恢复，不再称为“连续恢复”。限额与窗口从配置读取，必须为正数；生成具体项目时展示默认次数、窗口并询问使用者是否调整。 | 参考用户提供的示例思路，不原样复制其中的裸 IP/用户名 Redis key、`limit <= 0` 静默放行和在脚本外另查 TTL。保留私有键和既有统一 429/503、请求 ID：确实超额返回 429，Redis 无法判定时拒绝受保护操作，不能假装放行。固定窗口的边界允许窗口结束前后短时间内连续用两轮额度，具体项目若需要更严的平滑控制可自行替换。 |
| 验证码领取限流 | 五种图形验证码业务各自独立计数：登录和注册按业务加可信 IP；登录后的管理员新增、管理员重置、本人改密按业务加当前用户 ID。领取与刷新计入对应业务的同一份额度，默认从首次领取或刷新起 5 分钟内最多 10 张，到期后额度一次恢复，不再按 IP 和用户 ID 同时检查两层。超限不生成新图；刷新被拒绝时旧图不因此失效，单张图的 5 分钟有效期和单次使用规则不变。 | 没有验证码业务全站桶；登录、注册不根据表单用户名建立领取限额。10 张/5 分钟是生成项目时要展示并允许使用者调整的起点，不代表所有项目都必须使用这一强度。 |
| 全站共享限流 | 不论通用 API、登录、注册、图形验证码，还是按需启用的邮箱/短信验证码，都不能设置跨所有用户、IP 或不同业务共享的额度；同一 IP 跨全部 `/api/` 共用的 `api_ip` 也不能保留。只对具体需要保护的业务或接口，按上一行规则分别限流。 | 全站额度容易被一人故意耗尽而连累所有人；删除它后，多 IP 分布式攻击可能增加总负载。容量保护只能作为单独讨论的部署措施，不得暗中恢复全站共享额度。 |
| 登录、注册及临时密码完成提交限流 | 登录默认每个可信 IP 从首次提交起 5 分钟最多 20 次；注册默认每个可信 IP 从首次提交起 1 小时最多 5 次；临时密码完成默认每个可信 IP 从首次提交起 5 分钟最多 20 次，与普通登录使用不同业务键，不互相占用额度。三项匿名业务均不使用用户名或 IP 加用户名的第二层额度。用户名唯一性校验和登录密码校验仍正常工作，但它们不是限流主体。登录后的改密及管理员重置等操作按各自业务加操作者用户 ID。 | 去掉 `login_pair`、`registration_pair`、`registration_target` 等用户名维度，避免攻击者针对别人账号单独耗尽额度。这三项数字已获确认，是生成项目时要向使用者展示并允许调整的默认起点；其他接口的具体次数还须批量核对，不能把旧 Token Bucket 当成固定窗口。 |
| 管理员新增时的初始密码 | 创建时由管理员设置临时密码，通过可信渠道交给新用户；新用户先用它设置自己的正式密码，再正常登录。 | 复用现有临时密码流程，不强制增加邮箱。管理员知道临时密码，也可能抢先完成改密，因此必须限制权限、可信交付并记录审计；这不是防止管理员冒用的绝对保证。 |
| 忘记密码 | 基础 Skill 不提供“忘记密码”自助接口、邮件/短信找回流程及相应代码模板；忘记密码只能联系有权限的管理员人工核实并重置。唯一 `super_admin` 仍按线下受控恢复规则处理。具体项目确需自助找回时，由该项目自行设计和实现，不能把它混入基础模板。 | 用户名和图形验证码都不能证明账号属于请求者；保留管理员重置及用户完成临时密码的接口，不等于开放匿名自助找回。 |
| 多处登录与退出 | 同一账号允许多个有效登录；每次登录有独立的 Redis 活跃 JTI 和登录时间。生成项目时必须向其使用者沟通最多允许多少次同时有效登录，不在基础 Skill 中擅自写死数量。到达上限后默认使登录时间最早的旧会话失效，再启用新会话；这两步必须在 Redis 中原子完成，不能新登录失败却先踢掉旧会话。普通用户只能主动退出当前登录，不能自行调用“退出全部登录”；有相应权限的管理员可以强制退出严格低于自己等级的目标用户的全部登录，不能作用于自己、平级或更高级账号。成功改密或管理员重置仍自动让该账号的旧登录失效，但这不是对普通用户开放的“退出全部登录”接口。 | 只维护有效登录记录、各自登录时间和当前有效会话数量；不能把它称为准确的物理设备数量，同一手机重复登录也可能占多个名额。具体项目要识别设备需自行设计，基础 Skill 不开发设备指纹。现有逐 JTI 记录不足以执行上限和最早会话替换，须在 Redis 原子维护用户活跃登录索引；管理员强制退出或改密后，已因 `token_version` 变化而失效的旧 JTI 不得继续计入当前在线数量或占用上限。不加 PostgreSQL 会话表。管理员强制退出是针对目标用户的受保护操作，要复查权限与层级并写账号安全审计。 |
| 管理员读取范围 | `super_admin` 可以看非删除的用户和角色；其他管理员的管理列表、搜索、详情与导出只能看到比自己权限等级低的用户和角色，不能看到同级或更高级，也不能经管理接口看自己；本人权限从个人信息接口查看。 | 与“只能管理更低等级”一致，避免通过详情或搜索泄露平级和上级的权限；管理接口隐藏目标与不存在目标表现一致。 |
| 登录后的限流强度与修改入口 | 每个受限接口/业务单独计数，按服务端认证的用户 ID；默认普通查询 600 次/分钟、普通写入 120 次/分钟、管理查询 300 次/分钟、管理变更 60 次/分钟。管理员强制退出下级用户的全部登录属于管理变更，沿用该业务独立的管理变更额度；不另外设“每用户 30 次/10 分钟”。生成项目的 `app/settings.py` 集中声明次数与窗口，在 `.env.example` 列出可覆盖的 `RATE_LIMIT_*` 变量，在 README 明确告知使用者修改实际部署的 `.env` 对应参数后重启服务即可生效；不需要修改 Lua 或路由代码。生成具体项目时展示默认值并允许使用者调整，不把数字写死在路由或分散的依赖里。 | 已登录后可以定位并封禁滥用账号，但封禁是事后处理；较宽松的限流仍能减少账号被盗或程序死循环导致的短时损失。匿名、登录、注册、临时密码、验证码沿用各自已确认的额度；在线超级管理员转移接口不再存在，也没有其专门限流额度。 |
| 新用户角色 | 自行注册和管理员新增两条路径，都只默认绑定最低级的 `user` 角色。 | 不允许创建入口直接把新账号设为 `admin` 或 `super_admin`。 |
| 首位超级管理员 | 目标先通过普通注册取得 `user` 账号，再由开发人员核对该人的不可变 `users.id`，从可信环境手动执行受保护的 PostgreSQL 绑定脚本。 | 不自动提升第一位注册者，也不暴露 HTTP 初始化接口。人工执行脚本仍需同步处理权限版本和审计，不能只裸插入一条角色关系。 |
| 后续更换超级管理员 | 系统仍只有一位 `super_admin`；不开发超级管理员在线转移接口，也不保留其专门权限和限流配置。确需更换时由授权人员在可信环境手动执行受保护的 PostgreSQL 交接脚本，不由普通角色绑定接口完成。 | 脚本必须在同一事务中核对旧、新账号，将旧绑定失效并绑定新账号，保持最终恰有一位超级管理员，同时更新授权版本与审计；不要直接用几条无校验的散装 SQL，避免出现零位或两位以及旧权限继续生效。 |

## 修改前的实现差异（历史快照）

以下条目记录本次修改开始前发现的差异，供审查追溯；不描述当前工作树的最新实现。新功能是否通过验证，以测试结果和交付报告为准。

- 仓库随附的 PostgreSQL 示例中，`users.email` 与 `users.user_name` 都可为空，但至少有一个；现成的密码注册和登录接口只接受 `user_name`。这尚未与“基础模板只用用户名”的新决定统一。
- 当前用户名校验允许最多 160 个字符及许多非 ASCII 字符，且在校验前用 NFKC 转换全角字符；这与强制的 3～32 位 ASCII 字符规则不符，后续须统一调整入口、内部创建和数据库唯一性约束。
- 当前新密码要求 15～128 个字符，还会拒绝包含用户名或命中内置常见密码列表的密码；长度须改为建议的 8～60，包含匹配须收窄到完全相同，额外名单及其生产维护要求须从基础模板移除。
- 当前弱密码校验只通过名单拦截个别顺序串，没有通用的数字/字母顺序识别；用户名创建入口也尚未拦截上述 12 个保留名称。这两项新要求仍须落实到规范、示例代码和测试。
- 公开注册目前由默认 `false` 的 `PUBLIC_REGISTRATION_ENABLED` 部署配置决定；关闭时路由不会挂载。这既不是本次选择的默认开启，也不是能在后台页面实时操作、持久保存的开关，关闭时也没有“暂未开放注册”的明确响应。
- 随附资产有创建用户并绑定 `user` 角色的内部函数，但没有现成的“管理员新增用户”HTTP 接口。不能把该函数说成管理端功能已交付。
- 管理员新增用户还缺少专门的权限定义和验证，不能直接把内部创建函数暴露给 HTTP。未来的开关也须由服务端持久保存、按权限修改，并在注册请求真正写入前检查；不能只依赖部署环境变量或前端状态。
- 当前登录、注册、本人改密和管理员重置密码尚未接入图形验证码，管理员新增用户接口仍不存在，且管理员重置仍要求再次输入自己的密码。随附的可选验证码资产只覆盖邮箱/短信，没有图形验证码的实现。
- 当前限流规范和资产使用 Redis Token Bucket，不是已选定的简单固定窗口；需要同步规范、配置、Lua、依赖、中间件、业务接入及测试，不能只更换算法名称或示例。现有 `429001`/`503001` 响应与请求 ID 应保留。
- 当前仍有 `api_global`、`login_global`、`registration_global`、`verification_global`、`verification_submit_global` 五个全站共享桶及 `verification_channel` 跨用户共享桶；`api_ip` 虽按 IP 区分，但同一 IP 的不同 `/api/` 接口共用它，也与新决定不符。匿名业务读取策略 `anonymous_read` 不再需要，`authenticated_read`、`ordinary_write` 等按类别复用的键须改为各已登录业务独立。`rbac_state(scope='global')` 是 PostgreSQL 授权事务锁，与限流无关，不能误删。
- 当前匿名登录含 `login_pair`，注册含 `registration_target` 和 `registration_pair`，可选邮箱/短信含仅按目标、IP 加目标、仅按渠道等维度。这些限流主体均不符合“匿名业务仅按业务加可信 IP”的新决定；登录风险记录若继续保留，只能用于观测，不能充当按用户名拒绝登录的额度。
- 现有 Redis 登录资产只登记逐个活跃 JTI，没有按用户维护登录时间、当前有效会话数与上限的原子索引；实现本次决定时须补齐索引及最早旧会话的原子撤销，不能声称当前已限制同时登录数量或识别物理设备。
- 当前资产仍有在线超级管理员转移路由、服务、权限、专门限流与测试；首位超管手动初始化脚本不等于后续人工交接脚本。后续须删除在线转移入口及配套能力，并提供仅供授权人员线下使用的受保护交接脚本及验证，保留唯一持有人、版本和审计约束。
- 当前 `POST /auth/logout-all` 可由任何已登录用户对自己调用，并有专门的 `logout_all` 限流；这与新决定不符。后续须取消普通用户自行退出全部登录，改为仅有权限的管理员针对严格下级用户的受保护强制退出操作；普通用户的 `POST /auth/logout` 只撤销当前会话，改密/重置的自动全账号撤销保留。
- 现有密码参考文档仍把可选邮箱/短信自助找回列为产品选择；基础 Skill 后续须移除该自助接口的提问和实现承诺，同时保留管理员重置、临时密码完成与超级管理员线下恢复。现有资产并无“仅凭用户名自行找回”的公共接口。
- 本文只记录讨论结果，尚未更新 Skill 规范、示例代码、本机安装副本或 GitHub 发布版本；仓库里原有的未提交改动需保留。

## 待继续核对

### 账号与注册

- [ ] 用户名是否区分大小写（如 `Alice` 和 `alice` 是否同一账号）：在生成具体项目时询问其使用者；全角和其他非 ASCII 字符始终拒绝，不再作为可选规范化行为。
- [x] 后台注册开关初始开启；关闭仅阻止公开自助注册。
- [x] 只有 `super_admin` 能操作注册开关；普通 `admin` 的新增用户权限不能操作此开关。
- [x] 管理员新建用户时复用现有临时密码完成流程，可信交付；不新增邮箱依赖。
- [x] 临时密码不过期；仍须可信交付，完成改密或再次重置后旧密码失效。
- [x] 用户自行注册成功后只返回用户 ID，不自动登录，用户再单独登录。
- [x] 注册和管理员新增时，重复用户名统一提示“用户名已被占用”，不公开原账号是否已软删除。
- [x] 用户名去掉首尾空白后只允许 3～32 位 ASCII 英文字母、数字或下划线；内部空格及其他任何字符拒绝。
- [x] 新设密码默认建议 8～60 个字符；生成项目时询问大写、小写、数字、特殊符号是否各自必需，默认都不强制。
- [x] 拒绝顺序密码和与用户名相同的密码，即使达到最低长度。
- [x] 顺序密码只拦截整串连续递增或递减的数字、英文字母；不因内部包含短顺序片段就拒绝。
- [x] 只禁止密码与用户名完全相同；密码中包含用户名但不相同允许，拒绝时明确提示“密码不能与用户名相同”。
- [x] 用户名与密码仅大小写不同也视为相同；密码验证本身仍区分大小写。
- [x] 整串重复同一个字符的密码（例如 `aaaaaaaa`、`11111111`）必须拒绝。
- [x] 不再强制额外的常见/泄露密码名单；保留已确认的顺序、重复、与用户名相同三类拦截及清楚提示。
- [x] 基础模板禁止上表列出的 12 个敏感用户名，包括 `user`、`test`、`guest`、`ceshi`；实际角色不能成为例外。
- [x] 这 12 个词不区分大小写，只禁止完整用户名；包含敏感词但不完全相同的用户名允许。
- [x] 提交敏感保留用户名时提示“该用户名不可使用，请更换”，不当作已有账号占用。
- [x] 注册关闭时保留公开注册接口，但服务端拒绝新注册并提示“暂未开放注册”；有页面时隐藏入口或显示说明。
- [x] 前端通过公开 `GET` 读注册状态、超管通过受保护 `POST` 改状态；页面加载与操作后刷新，服务端提交时作最终检查。
- [x] 不提供匿名普通业务查询或变更；业务 API 默认先验登录。未登录只允许明确列出的认证入口及仅返回开关的公开注册状态接口，健康检查不包含业务数据。
- [x] 管理员新增/重置需登录、权限和验证码，不再强制重新输入管理员当前密码；本人改密仍须旧密码，且也要验证码。
- [x] 登录、注册、管理员新增用户、管理员重置他人密码、本人改密五种场景必须使用图形验证码；项目若要求更高强度，可自行替换；不暗中增加邮箱或手机号。
- [x] 图形验证码自生成起有效 5 分钟；过期后须重新获取。
- [x] 每张图只允许提交一次；正确或错误都须原子校验并删除该验证码关联的所有 Redis key，不能确认消费时不执行受保护操作；失败后获取新图，不设置“五次错误”额度。
- [x] 图形验证码按五种业务用途隔离，登录、注册、管理员新增用户、管理员重置他人密码和本人改密之间不可混用；用途不匹配消费提交的验证码，不影响其他业务的验证码。
- [x] 登录后绑定领取者的当前用户 ID，由服务端认证取得；未登录的登录、注册只按用途隔离，并用独立的不可猜测挑战标识区分每张图，不强加用户 ID。
- [x] 主动刷新时旧图立即失效；服务端原子替换，同一旧图的并发刷新仅能有一次成功。匿名请求没有用户身份，只能替换其明确携带标识的旧图。
- [x] 验证码领取和刷新接口必须限流，且保留登录/注册提交时按个体维度的限流。
- [x] 图形验证码领取与刷新按五种业务分别限流：未登录按固定业务加可信 IP，已登录按业务加服务端认证的用户 ID；不叠加第二层主体，不用请求体里的用户名，也不设全站桶。
- [x] 图形验证码领取与刷新共用该业务的一份默认额度：首次领取或刷新起 5 分钟最多 10 张，到期一次恢复；与单张验证码的 5 分钟有效期相互独立，不再采用 Token Bucket 连续恢复。
- [x] 所有限流接口都不设置全站、全渠道或跨全部业务共享的 IP 总额度；每个受限业务使用一个独立固定窗口键。
- [x] 未登录的登录、注册及临时密码完成按各自业务加可信 IP 限流；删除仅按用户名、IP 加用户名等请求体目标维度，已登录敏感操作按业务加操作者用户 ID。
- [x] 简单固定窗口通过 Lua 原子计数和设置过期时间；不得裸拼个人信息到 Redis key、无效限额不得静默放行，Redis 无法判定时不得绕过限流。
- [x] 登录默认每 IP 5 分钟 20 次、注册默认每 IP 1 小时 5 次、临时密码完成默认每 IP 5 分钟 20 次；三项各自独立固定窗口，生成具体项目时仍需向其使用者展示并允许调整。
- [x] 不提供匿名“忘记密码”自助接口；忘记密码联系管理员人工核实并重置，用户用临时密码设置正式密码；额外自助找回由具体项目自行设计，基础 Skill 不承担其接口开发。
- [x] 允许同一用户多处登录；生成项目时询问最多同时有效登录数，并说明默认到达上限时原子淘汰最早旧登录、允许其使用者调整，不把登录次数误说成可精确识别物理设备。
- [x] 管理员管理端的读取只看严格低于自身权限等级的目标；本人通过个人信息接口看自己的权限。
- [x] 已登录普通查询每接口 600 次/分钟、普通写入 120 次/分钟、管理查询 300 次/分钟、管理变更 60 次/分钟；要在项目配置、环境变量示例和使用说明中明确修改入口，生成项目时仍向其使用者展示。
- [x] 只有有相应权限的管理员能强制退出严格下级用户的所有登录；普通用户只能主动退出当前登录，不能调用退出全部接口。管理员操作按管理变更额度，不单独设置“30 次/10 分钟”；改密/重置导致的自动失效仍保留。
- [x] 取消在线超级管理员转移接口、权限及“3 次/小时”专门限流；首次绑定和以后交接分别采用线下受保护 PostgreSQL 脚本，最终始终只有一位持有人。
- [x] 登录后的图形验证码只绑定业务、用户 ID 与挑战标识，不再额外绑定物理设备或本次登录的 JTI；不把图形验证码当作本人身份凭据。
- [ ] 仅在实际项目选择按需邮箱/短信验证码时核对其次数和窗口；多 IP 同时请求的总负载不再由应用内全站桶兜底。
- [x] 第一位 `super_admin` 不是第一个注册者自动获得权限；开发人员对已注册的正确账号手动执行受保护的数据库绑定脚本。
- [ ] 仅在改造已有项目且确有无密码账号时，询问如何核实本人并启用；新项目不为不存在的存量账号设计迁移。现有超级管理员引导脚本只会绑定已存在的账号，不会创建账号或补设密码。

### 留待实际项目或后续核对的参数

下表只保留尚未确认的具体数值。不能把建议起点当成已生效的 Skill 行为；已经确认的参数不在此重复询问。

| 主题 | 推荐默认值（未确认） | 要权衡的实际影响 |
| --- | --- | --- |
| 同时有效登录上限 | 生成项目时询问使用者最多允许多少个活跃会话，必须选择正整数。达到上限时默认淘汰最早的旧会话，这一点已经确认；只有上限数字需要实际项目决定。 | 记录活跃会话及登录时间，不识别物理设备。当前随附配置 `MAX_ACTIVE_SESSIONS_PER_USER` 必填且只接受正整数，不支持“不限制”；具体项目若确有无限制需求，须单独变更设计、代码和测试。 |
| 按需验证能力 | 只有实际项目明确选择邮箱/短信验证码时，才讨论其独立的额度；基础用户名密码模板不增加此类验证码。没有匿名普通查询、在线超管转移或用户自行退出全部登录的专门限流数字。 | 不为未提供的功能设置无用参数。 |

### 之后按顺序核对的主题

- [ ] 密码规则、改密、管理员重置和临时密码每一步的用途与代价；不再开发忘记密码自助接口。
- [ ] 登录有效期、多设备、JWT、Redis 活跃 JTI、退出当前设备与退出全部设备。
- [ ] 三个系统角色、管理员层级、授权接口、角色数上限、并发写入和软删除生命周期。
- [ ] 通用接口限流、注册与登录防爆破的次数、异常处理和图形验证码的领取限额与操作流程。
- [ ] 统一响应格式、运行日志、权限审计、账号安全审计和按需业务审计。
- [ ] 非顺序 ID、可选国家表和代理检测、数据库权限及实际部署验证。

对每个主题都应区分：Skill 的建议、随附代码真正实现的能力、仍需使用者决定的产品行为。当前讨论期间不改临时密码流程，不推送 GitHub 或发布新版本。
