# 发布检查清单

[English](RELEASE_CHECKLIST.md) | **简体中文**

本清单用于首次公开预览版及后续发布。命令、版本、标签和 URL 应与英文清单保持
一致。

## 本地发布包

- [ ] 随附 Skill 通过当前 `skill-creator` 快速验证器。
- [ ] 可选国家 CSV 校验器的聚焦单元测试通过，并且测试过程不会修改或捆绑源数据集。
- [ ] Ruff 格式与代码检查、严格 mypy、`pip-audit` 和非 PostgreSQL 测试全部通过。
- [ ] 测试依赖保持 `pytest>=9.0.3,<10` 和 `pytest-asyncio>=1.4,<2`，CI 在安装
  和扫描前升级到 `pip>=26.2`。如需降低版本，必须有新的无漏洞扫描结果，或在
  中英文 Security 文档和 CI 中都明确记录临时例外。
- [ ] 带 PostgreSQL 标记的测试在 PostgreSQL 17 上通过。
- [ ] 依赖 Redis 的认证测试在 Redis 7 活跃 JTI 实例上通过；限流、登录/注册防滥用和必需
  的图形验证码测试在单独配置的限流 Redis 上通过。单独的验证码／会话 Redis 测试
  还要使用第三个空逻辑库，不能占用后续集成测试的两个目标。确认 CI 的三个 URL。
  在所有 PostgreSQL/Redis 测试组
  真正通过前，不得声称 PostgreSQL/Redis 行为已经验证。
- [ ] 本地密码测试覆盖 Argon2id 线程外执行、真实或 dummy 校验、`users` 密码字段、
  注册与登录、本人改密、管理员默认直接重置与可选临时重置、临时密码完成、
  离线操作者找回与超级管理员交接、
  超额后退出最早登录、Token 失效、权限层级、原子账号安全审计，以及响应和日志不泄露秘密。
- [ ] 发布树中不存在 `.env`、Token、凭据、私钥、缓存、数据库或构建产物。
- [ ] 仓库、Skill 和复制资产的边界均包含 `LICENSE`、`NOTICE` 和
  `THIRD_PARTY_NOTICES.md`。
- [ ] README 描述的限制与资产实际实现保持一致。
- [ ] 中英文 README 都将 Python 3.12、PostgreSQL 17、Redis 7 写为官方验证环境，
  链接中文架构总览，并说明 `/health/live` 与 `/health/ready` 的不同用途。就绪契约
  必须包括准确迁移 head、全局 `rbac_state`、两套 Redis 的 `PING` 与 Lua 写读删、
  禁止缓存和不泄露内部细节。
- [ ] 完成所有本地检查后，从最终干净工作树运行
  `python -B scripts/validate_release.py` 并通过。

## v0.6.0 验收基线

- [ ] 全部面向客户端的运行时 API 文案和安全参数校验明细都使用稳定消息键，并有
  完整 `zh-CN`、`en` 资源。语言缺失或不支持时回退中文；有界的标准
  `Accept-Language` 协商返回规范化 `Content-Language`，且只合并一次
  `Vary: Accept-Language`，不改变四字段响应结构。更具体的 `q=0` 排除规则优先于
  父级语言范围或 `*`。
- [ ] 两份资源键完全一致；恶意或超长语言头不能选择文件路径、动态导入、Redis Key、
  日志或审计值。中英文并发请求不能串语言。HTTP／业务码、响应数据、请求 ID、
  安全响应头、日志、审计字段、权限键和内部原因码均保持稳定且不翻译。数据库中由
  业务人员维护的内容和面向开发者的 OpenAPI 元数据明确不属于运行时文案翻译范围。
  参数校验字段提示不能回显额外字段或更深层字典键，未捕获的 `500` 仍须返回两项
  语言响应头。
- [ ] 生成新项目前解释并要求选择一种项目级管理员重置密码模式：明确默认
  `direct`，新密码立即成为正式密码；可选 `temporary`，用户必须完成一次正式密码
  设置。接口统一接收 `new_password`，不允许调用者按单次请求选择模式。
- [ ] 两种模式都要求 `users:password:reset`、`admin_reset` 图形验证码和可见的严格
  低级目标，不能重置自己、平级或上级。两者都增加 `token_version`、撤销旧 Token，
  使用同一审计动作配合不同原因码原子记录，且密码不进入响应、日志或审计快照。
- [ ] 默认直接模式的集成测试证明新密码无需额外步骤即可登录；可选临时模式证明
  完成一次性正式密码设置前登录不签发 Token。数据库结构和迁移保持不变。

## v0.5.1 验收基线

- [ ] 新项目以必填 `user_name` 登录：先去首尾空白，只允许 3～32 位 ASCII 字母、
  数字、下划线；敏感完整用户名和软删除的用户名不能再次注册。一次性询问用户名
  是否区分大小写、8～60 字符之外的密码组成要求、同时登录上限的正整数、本次相关
  限流默认值、是否成对启用 `iss`/`aud`，以及是否由专业运维分离 PostgreSQL 迁移
  与运行角色。只有存量项目才询问没有本地密码的旧账号如何接入。已有项目保留原有
  身份契约。
- [ ] PostgreSQL 注册开关初始为开启，仅超级管理员可修改；每次公开注册均在事务中
  复查。未登录查询只返回一个开关状态；关闭注册不能影响登录或有权限的管理员新增
  用户。公开注册与管理员新增都只授予 `user` 角色。
- [ ] 登录、注册、管理员新增用户、管理员重置别人密码、本人改密共五种图形验证码
  不可串用；答对、答错都在一次提交中原子消费。五分钟过期，刷新只替换指定旧图；
  每场景、每主体五分钟最多领取 10 张。生成图片失败不得使旧验证码失效；合法用途
  发错入口或已解析但字段无效的请求走独立的按主体拒绝额度，不占正常领取额度。
  基础模板不附带邮箱／短信发送模块或忘记密码自助接口。
- [ ] 普通用户只能退出当前登录；登录查询只返回会话数量和登录时间，不声称识别
  物理设备。成功的新登录超过项目选定的上限时退出最早登录。有权限的管理员只能
  强制严格下级的全部登录失效，不能针对自己、同级或更高用户。
- [ ] 唯一 `super_admin` 首次绑定与以后交接都通过受保护、审计、事务化的线下 SQL；
  没有在线转移接口，也没有独立的 `can_delegate` 开关或管理 API。授予者不能授出
  自己没有的权限。盘点全部表和所有数据移除路径：可变实体软删除、审计只追加，
  每人最多十个角色且禁止自行提权，均在锁内校验。
- [ ] 所有限流都没有全站或跨业务额度，包括可选国家和代理说明。未登录的认证
  业务按业务加可信 IP，已登录业务按业务加用户 ID；Redis 简单固定窗口无法判断时
  拒绝放行。验证 `429001`、`Retry-After` 和可调整的默认次数；提交的用户名不是
  匿名限流主体。可选国家目录不能默认作为匿名普通业务查询接口。
- [ ] JWT 默认一小时、最少声明和 Redis 活跃 JTI；无逐 Token 的 PostgreSQL 表。
  `iss`/`aud` 必须经使用者明确同意。密码更改使旧 Token 失效；请求 ID、日志脱敏、
  RBAC 与账号安全审计的原子提交以及软删除仍保留。

下方 `v0.4.0` 是已发布版本的历史检查记录，不应用其中的邮箱选择、在线移交、
全站限流或本人退出全部登录等旧规则验收 `v0.5.1`。

## GitHub 仓库

- [ ] 在 `https://github.com/xtnkking/fastapi-templates-xtn` 创建公开规范仓库，
  默认分支为 `main`。
- [ ] 不向外部用户、团队、GitHub App 或部署密钥授予 write、maintain 或
  administrator 权限。
- [ ] 为维护者账号启用双重身份验证（2FA），并检查个人访问令牌和已授权应用。
- [ ] 启用私密漏洞报告。
- [ ] 为 `main` 启用规则集，禁止删除和强制推送、要求 CI，并且只允许 XTN
  绕过规则。
- [ ] 保护 `v*` 标签，禁止删除或替换。
- [ ] 保持 Issues 开启；按照[贡献说明](CONTRIBUTING.zh-CN.md)关闭外部 Pull
  Request。

## 发布 v0.6.0

- [ ] 审查完整差异、中英文 README、中英文更新日志、发布清单和中文架构总览；
  资产元数据、安装 URL、更新日志与发布校验器统一使用 `0.6.0` 和日期
  `2026-09-20`。
- [ ] 确认 `v0.6.0` 没有新增数据库结构迁移，也没有修改 `v0.5.0` 已发布的四个
  迁移。后续若改变表结构，必须新增向前 Alembic 迁移，不能改写发布历史。
- [ ] 运行 Ruff 格式与代码检查、严格 mypy、`pip-audit`、非 PostgreSQL 测试、发布
  校验器和 Skill 快速校验；让 GitHub Actions 在准确发布提交上运行可丢弃的
  PostgreSQL 17 / Redis 7 集成测试和迁移检查。
- [ ] 确认最终 CI 日志实际使用 `pip>=26.2`、解析到 `pytest>=9.0.3,<10` 与
  `pytest-asyncio>=1.4,<2`，并且 `pip-audit` 没有未记录的忽略项。
- [ ] 提交并推送 `main`，等待该准确提交的两个 CI 任务通过；在同一提交创建并推送
  不可变 `v0.6.0`，不得替换或移动任何旧标签。
- [ ] 将 `v0.6.0` 发布为非草稿、非预发布的 GitHub Release，再验证源码压缩包和
  标签 Skill 安装 URL。

## 发布 v0.5.1

- [ ] 审查完整差异、中英文 README、中英文更新日志、当前发布清单、归档后的 v0.4.0
  设计记录和中文架构总览；资产元数据、安装 URL、更新日志与发布校验器统一使用
  `0.5.1` 和日期 `2026-09-19`。
- [ ] 确认 `v0.5.1` 没有新增数据库结构迁移，也没有修改 `v0.5.0` 已发布的四个迁移。
  后续若改变表结构，必须新增向前 Alembic 迁移，不能重写发布历史。
- [ ] 运行 Ruff 格式与代码检查、严格 mypy、`pip-audit`、非 PostgreSQL 测试、
  可丢弃 PostgreSQL 17 / Redis 7 集成测试、迁移升级-降级-再升级、发布校验器和 Skill
  快速校验；同时验证就绪探针检查准确迁移 head、全局 `rbac_state`、两套 Redis 的
  `PING` 与 Lua 写读删、Key 清理、禁止缓存和不泄露，并验证所有显式路由限流映射。
- [ ] 确认最终 CI 日志实际使用 `pip>=26.2`、解析到 `pytest>=9.0.3,<10` 与
  `pytest-asyncio>=1.4,<2`，并且 `pip-audit` 没有未记录的忽略项。
- [ ] 提交并推送 `main`，等待该准确提交的两个 CI 任务通过；在同一提交创建并推送
  不可变 `v0.5.1`，不得替换或移动 `v0.5.0` 及更早标签。
- [ ] 将 `v0.5.1` 发布为非草稿、非预发布的 GitHub Release，再验证源码压缩包和从
  标签路径执行的全新 `$skill-installer` 安装。

## 发布 v0.5.0

- [ ] 审查完整差异、中英文文档、上游署名与新项目迁移边界。确认更新日志、资产版本、
  安装链接和发布校验器均使用 `0.5.0` 及对应日期。
- [ ] 发布树校验、Ruff、严格 mypy、单元测试，以及独立 PostgreSQL 17 和两套 Redis
  集成测试均通过。确认 Redis 单项测试使用第三个独立逻辑库，不会给后续集成
  安全检查的两个目标留下数据。
- [ ] 提交并推送 `main`，等准确提交的两个 CI Job 都通过后，在同一提交上创建并
  推送 `v0.5.0` 标签，不替换旧标签。签名密钥是可选建议，不是发布条件。
- [ ] 将 `v0.5.0` 作为非草稿、非预发布的 GitHub Release 发布，突出用户名唯一
  登录、五种验证码、按业务限流、线下超级管理员交接和密码表变化等破坏性变更。
- [ ] 验证公开标签、GitHub 源码下载，以及从标签路径全新安装 Skill。仅当另行
  上传发布归档时才需要附加校验和。

## 发布 v0.4.0

- [ ] 验证 Skill 会附带默认答案，一次性询问尚未确定的认证业务问题：身份与登录字段、
  注册方式、经过验证的找回凭据与渠道、多设备登录和已有账号接入。不得把固定的
  Argon2、dummy 校验、事务、审计、脱敏或旧 Token 撤销规则交给使用者选择。
- [ ] 验证本地密码资产公开约定的五个 POST 命令，在
  `users.password_hash` 字段中保存 Argon2id，用户软删除时清空哈希，对不存在账号做
  真实 dummy 工作并返回统一登录错误，绝不把用户名当成归属证明。认证只使用一个短期
  Access Token，到期后重新登录，不增加逐 Token 的 PostgreSQL 表。
- [ ] 验证改密、重置和完成找回都会递增 `users.token_version`，并与只追加的账号安全
  审计一起提交；改密次数只统计成功审计动作，不增加独立密码版本或计数字段。
  管理员重置不再要求操作者重复输入当前密码，但必须具备 `users:password:reset`，
  并执行完整的严格低层策略；唯一 `super_admin` 只能通过交互式离线操作者命令找回。
- [ ] 验证旧的 Python 引导入口已经移除，随附 SQL 要求目标账户已经处于活跃状态并
  拥有 `user`；目标 PostgreSQL 数据库必须由使用者本人操作。测试首次绑定、同一
  用户重复执行、无效身份、不同持有人、版本与全局 epoch、审计和完整回滚。
- [ ] 验证绿地项目会在用户表设计前询问 `users` 存储 `email`、`user_name` 或
  两者，并分别记录各创建流程的必填规则、登录输入、规范化、唯一性、跨命名空间
  歧义和删除后复用策略。除非使用者明确要求迁移，否则保留存量项目的身份契约。
- [ ] 盘点全部表和所有数据移除路径。允许运行时变化的实体及关系事件必须写入软删除
  墓碑；只追加审计记录和永久的 `rbac_state`/系统目录不得提供删除路径。任何表都
  不能保留未明确声明的硬删除默认行为。
- [ ] 验证初始化会询问是否有专业 PostgreSQL 运维能力支持“拥有 schema 的迁移角色 +
  受限运行时角色”。选择分离时，验证运行时角色不能直接删除或清空软删除数据；不选择
  时，允许小型或学习项目继续推进，并记录 owner/superuser SQL 可绕过应用软删除，
  不得把这项可选加固标记为已通过。
- [ ] 验证新项目指南优先使用已登记的业务前缀和按生命周期体量选择长度的随机大写
  后缀，同时明确已有 UUIDv4 项目和随附 UUIDv4 资产仍然合规。任何业务或 RBAC ID
  都不得使用整数序列或公开的顺序别名。
- [ ] 验证 Access Token 默认有效期可配置且为 3600 秒，并在交付说明中告知使用者
  结合业务风险和登录体验进行调整。
- [ ] 验证 JWT 默认严格只有 `sub`、`jti`、`iat`、`exp` 和 `token_type`。新项目加入
  可选的 `iss`/`aud` 前，Skill 必须先用大白话说明跨服务 Token 限定的收益与配置
  成本，并取得使用者明确同意；未回复不算同意。验证只配置其中一个会被拒绝，存量
  项目已经同时配置时默认保留。
- [ ] 验证启动时不仅拒绝少于 32 个 UTF-8 字节的 JWT 密钥，还拒绝公开示例占位值和
  明显薄弱的密钥。验证传入的 Bearer Token 与生成的 Access Token 均限制为 4096 个
  UTF-8 字节，包括多字节输入，以及在注册 Redis 前拒绝超限的生成 Token。
- [ ] 验证登录仅在一次 Redis 5.0+ Lua 操作使用服务器 `TIME`、`SET NX EX`
  和 `PEXPIREAT` 写入精确的活跃 JTI 值、且过期时间不晚于 JWT 后才返回 Token。
  认证严格按照 JWT、Redis、现有 PostgreSQL 用户/RBAC 的顺序执行。
- [ ] 验证没有增加逐 Token 数据库表或逐请求 Token 记录查询。
- [ ] 验证只有确认精确删除 Redis 记录后才返回注销成功；测试 Key 缺失或不匹配、
  Redis 故障和结果未知、已经通过门禁的在途请求，以及故障转移恢复旧快照。
- [ ] 验证 `POST /api/v1/auth/logout-all` 会锁定当前用户、把 `users.token_version`
  恰好递增一次，并使使用旧版本签发的全部 Token 在下次认证时失败，且不扫描或批量
  删除 Redis Key。覆盖用户不存在、已停用或版本已经不匹配，以及递增前已通过认证的
  在途请求。
- [ ] 生成限流或登录/注册防滥用前，验证 Skill 会展示当前功能适用的全部集中默认次数，
  并询问使用者是接受全部默认值还是只列出修改项。只有使用者明确选择 CAPTCHA、邮箱/
  短信验证码、MFA 或其他加强验证后，才展示对应的可选默认值；未回复不能当作同意。
  记录最终确认的配置，并要求结合真实部署再次审核系统总量和供应商总量。
- [ ] 验证最外层 ASGI 在昂贵业务处理前，通过一次 Redis 原子判断同时应用系统总量和
  可信客户端 IP 桶；未配置可信代理契约时，伪造转发头不能改变 IP。认证后限流必须按
  已记录的读/写/管理/退出策略选择，`super_admin` 也不能绕过；Redis Key、日志、指标和
  响应不得暴露原始 IP、账号、用户 ID、Token 或 JTI。
- [ ] 验证 `APP_ENVIRONMENT` 没有代码默认值且始终必填，即使限流保持启用也一样；并验证
  除明确的本地和测试环境名以外，设置层都会拒绝 `RATE_LIMIT_ENABLED=false`。公开注册
  默认必须同时从路由表和 OpenAPI 中消失，只有 `PUBLIC_REGISTRATION_ENABLED=true` 时才出现。
- [ ] 验证真正超限会返回 HTTP `429`、业务码 `429001`、统一四字段正文、相同的
  `X-Request-ID` 和整数 `Retry-After`，并且不会继续执行受保护逻辑。限流 Redis 不可用、
  超时或返回损坏状态时，敏感流程必须关闭放行并返回 `503001`，不能伪装成普通超限。
- [ ] 验证登录硬限制只有 IP、IP+规范化账号和系统总量；注册独立使用 IP、规范化目标、
  IP+目标和系统总量。不存在与存在的登录身份必须使用一致的公开结果，并执行真实密码
  哈希或固定 dummy 哈希。登录失败风险信号会原子更新、自动过期并在成功时清除，但不能
  单独产生账号级 `429`，也不能阻止系统校验正确密码。
- [ ] 验证纯用户名密码配置无需验证码 HMAC 密钥、用途、渠道、供应商、邮箱/手机号要求或
  验证码路由即可启动和运行。使用者没有明确选择时，不得加入 CAPTCHA、邮箱/短信验证码
  或 MFA。
- [ ] 使用者选择邮箱/短信验证码后，验证验证码使用 `secrets` 生成并保留六位格式，严格
  绑定 challenge ID、用途、渠道和规范化目标，Redis 只保存 HMAC。测试 5 分钟到期、
  重发替换、第五次输错立即失效、并发只能成功一次、发送失败清理但不退还额度、不会
  枚举账号的错误，以及不会泄露验证码、目标、供应商响应或凭据的发送适配器边界。
- [ ] 验证所有普通 JSON 响应都只包含 `code`、`message`、`data` 和由服务端生成的
  UUIDv4 `request_id`；成功与所有错误路径的 `X-Request-ID` 必须相同，并且不得
  采用调用方传入的 ID。
- [ ] 验证列表接口只返回 `items`、`page`、`page_size`、`total`，页码与每页数量
  默认分别为 1 和 20，每页最大 200，统计使用相同筛选条件且排序稳定。
- [ ] 验证普通管理员只能列出或查看有效权限严格低于自己的未删除用户和角色；同级、
  更高级、受保护目标和自己必须隐藏为与资源不存在相同的 `404`。验证 `super_admin`
  可以读取全部未删除用户和角色，并且过滤后的总数与可见结果集一致。
- [ ] 验证角色和用户列表分别批量加载授权与访问视图：页面大小从 1 增至 200 时，
  SELECT 数量不得增长。用户响应必须区分 `assigned_role_ids`、`effective_role_ids` 和
  生效权限；禁用角色仍保持分配，但不贡献权限层级或权限。
- [ ] 验证角色写入要求请求体中的严格整数 `expected_version`，授权拒绝必须先于
  版本冲突判断，已获授权的陈旧写入返回 HTTP `409` 和 `409002`，现行路由及
  OpenAPI 不再要求 ETag/`If-Match`。
- [ ] 验证每个用户最多 10 个未解绑角色：必需的 `user`、可能持有的 `super_admin`
  和已禁用角色都计数，软删除历史不计数。覆盖第 10 个成功、第 11 个拒绝、重复绑定
  幂等、解绑后复用名额、首次超级管理员引导、超级管理员移交、直接 SQL、干净升级到
  唯一的 `0004_password_auth` head，以及两个并发写入争抢第 10 个名额；同时确认数据库
  防线源自 `0001`，并由服务层和 PostgreSQL 共同校验最终数量。
- [ ] 验证权限目录不包含 `roles:permissions:update` 或 `system_owner:transfer`，公开响应
  与 OpenAPI 只使用 `super_admin` 术语，不暴露 `is_super_admin` 或应用级 ownership 字段；
  不得把它与 PostgreSQL 合法的 owner/superuser 术语混为一谈。
- [ ] 验证每条响应路径只产生一个结构化请求完成事件，且服务端请求 ID 与响应一致，
  同时包含路由模板、HTTP/业务码和单调时钟耗时；字段必须有界，并且不能包含请求体、
  查询串、请求头、Token/JTI、凭据、邮箱、用户名、代理 URL 或数据库/Redis URL。
- [ ] 验证必需的 RBAC 审计包含可信的操作者、目标、动作、决定、原因、来源、Schema
  字段和有界安全快照；允许操作的审计失败必须回滚业务变更，拒绝审计失败必须保留
  原始的 `403`、`404` 或 `409` 状态与原因，并且 PostgreSQL 必须拒绝直接更新或删除
  审计记录。
- [ ] 验证业务审计与 RBAC 审计保持独立，并且只记录明确的重要动作目录。测试资源
  身份、逐动作状态字段白名单、`succeeded`/`failed`/`denied` 结果、成功写入的原子
  回滚、业务回滚后的失败记录、提交结果未知处理、数据库只追加控制、访问及保留策略，
  以及敏感数据拒绝行为。
- [ ] 验证国家目录保持按需启用且不进入默认 RBAC 资产。测试精确核对产品认可的
  国家码集合、字符串且不唯一的国际区号、原子幂等 UPSERT、软删除冲突不会暗中
  恢复、只读取有效数据，以及文件缺项不会触发删除。确认 `--structure-only` 返回
  `membership_checked=false` 且不能批准导入。只要存在非空 `flag_url`，必须显式
  提供经批准的 ASCII DNS 主机名白名单。没有记录来源和再分发许可时，不得发布源
  CSV/TSV 或旗帜资产。

### 发布操作

- [ ] 审查暂存差异、双语文档、XTN 版权年份和上游提交，确认资产版本、发布校验器
  与更新日志中的 `v0.4.0` 和发布日期一致。
- [ ] 提交并推送已审查的工作树。仅在 CI 对应提交通过后，才在该准确提交上创建
  `v0.4.0` 标签。有可迁移的密钥管理方案时建议签名，但不是发布的强制条件。
- [ ] 将 `v0.4.0` 发布为 GitHub Release。把 JWT 声明契约、严格的管理员读取范围、
  最多 10 个角色绑定和角色版本前置条件的变化标明为破坏性变更；重点说明 Redis 活跃
  JTI 验证、Argon2id 密码流程、限流和独立审计模块。
- [ ] 如果手动上传发布归档，请附加 SHA-256 校验和。
- [ ] 在干净的 Codex 环境中，通过公开 GitHub 标签树 URL 验证安装。

## 后续分发

- [ ] 当独立 Codex Skill 工作流之外还需要更广泛的公开安装能力时，将 Skill
  打包为 Plugin。
