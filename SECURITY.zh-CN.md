# 安全策略

[English](SECURITY.md) | **简体中文**

> 本页是 `SECURITY.md` 的简体中文对应文档。如解释存在差异，以英文安全策略为准。

## 支持的版本

在第一个稳定版发布之前，安全修复只应用于默认分支上的最新代码，以及存在发布版
时的最新发布版本。XTN 不支持更早的提交、标签、派生仓库或修改后的副本。

## 报告漏洞

请在官方仓库的 Security 页面使用 GitHub 私密漏洞报告：

https://github.com/xtnkking/fastapi-templates-xtn/security/advisories/new

如果私密漏洞报告功能不可用，请创建一个标题严格为
`Security contact request` 的公开 Issue，但不要包含任何漏洞细节。维护者将安排
私密沟通渠道。请勿在公开 Issue 中披露尚未修复的漏洞、利用方法、凭据、Token、
个人数据或具有破坏性的概念验证。

在条件允许时，请提供受影响的文件及版本或提交、影响、复现步骤、必要前置条件，
以及非破坏性的概念验证。请同时说明该问题是否已经公开或已报告给其他组织。

XTN 会在可用时间允许的范围内确认收到报告、评估问题，并协调修复与披露。项目
不保证响应或解决期限。公开披露前，请预留合理的修复时间。

## 依赖漏洞扫描的临时例外

CI 使用 `pip-audit` 扫描依赖；正常情况下，只要发现已知漏洞就不能发布。只有在
暂时没有可用修复方案，并且维护者已经核对真实影响时，才允许建立临时例外。例外
必须同时明确记录在仓库和 CI 的准确 `--ignore-vuln` 配置中，不能只在个人电脑上
忽略，也不能静默跳过。

`v0.5.1` 已通过要求 `pytest>=9.0.3,<10` 与兼容的 `pytest-asyncio>=1.4,<2`，
以及让发布 CI 在安装和扫描前升级到 `pip>=26.2`，处理当前发现的工具链漏洞。
如果降低其中任何一项，必须重新扫描。

每条例外必须写全以下信息：

- 漏洞编号以及受影响的依赖和版本；
- 对本 Skill 和生成项目的影响；
- 暂时不能升级、替换或删除该依赖的原因；
- 负责人；
- 明确的复查日期。

一旦存在可用修复，负责人应立即删除例外。超过复查日期的例外，必须在发布前重新
评估或删除。当前没有任何依赖漏洞扫描例外。

## 范围

报告范围可以包括 Skill 指令、随附的 FastAPI/PostgreSQL RBAC 起始项目、
Redis/JTI 设计指南、身份认证与授权行为、迁移以及规范仓库中的发布自动化。
第三方依赖中的漏洞也应同时报告给相应的上游项目。

随附的起始项目是参考实现材料，并非托管服务，也不保证由它生成的应用在每种部署
环境中都安全。使用者需要自行负责威胁建模、配置、依赖更新、密钥管理、测试和
运行控制。
