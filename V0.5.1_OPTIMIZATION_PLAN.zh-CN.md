# fastapi-templates-xtn v0.5.1 优化计划

状态：实施中，待隔离集成测试与发布  
制定日期：2026-09-19  
目标版本：`v0.5.1`

## 1. 本文的作用

本文是 `v0.5.1` 的执行基线，用来防止开发、文档整理、测试和发布过程中偏离已经确认的目标。

- 实施时必须逐项完成并更新本文中的复选框。
- 发现新问题可以补充，但不得借机扩大产品范围。
- 若准备改变本文规定的行为、接口或明确不做的功能，必须先说明原因、影响和替代方案，并取得 XTN 明确同意。
- 不修改、覆盖或重新指向已经发布的 `v0.5.0` 标签。
- 本版本以修复和维护为主，不引入与现有使用者无关的大型架构。

## 2. 版本目标

`v0.5.1` 必须完成以下结果：

1. 修复已确认的四项实现问题：匿名登录/注册限流顺序、管理员强制下线限流档位、用户管理缺少用户名、会话响应缓存控制。
2. 补齐可直接用于部署探针的就绪检查。
3. 让 Skill 默认提示、主入口、参考文档、中文说明和实际代码保持一致。
4. 降低每次加载 Skill 时的固定 Token 消耗，同时保留所有安全底线。
5. 加强 CI 和发布校验，避免同类问题再次出现。
6. 完成隔离的 PostgreSQL、Redis 和 API 行为验证后，再同步本机 Skill 并发布。

## 3. 实现修复

### 3.1 匿名登录和注册的限流顺序

- [x] 将登录和注册的固定窗口限流放在图形验证码校验之前。
- [x] 固定执行顺序为：可信来源 IP 检查及业务限流、图形验证码、密码校验或注册事务、Token 签发。
- [x] 错误答案、随机 `captcha_id`、过期验证码和用途错误都必须已经消耗本次登录或注册额度。
- [x] 限流超额或限流 Redis 无法给出可信结果时，禁止继续读取验证码、校验密码、写 PostgreSQL 或签发 Token。
- [x] 保持验证码一次性规则：真正进入验证码校验后，无论答案正确或错误都消费该验证码。
- [x] 保持无全站额度、无用户名维度、无 IP 加用户名第二层额度。

验收标准：

- 无效验证码连续请求最终会得到 `429001`。
- 限流拒绝发生时，验证码不会被提前消费。
- 限流 Redis 故障返回 `503001`，且验证码、Argon2、注册回调和 Token 签发均未执行。
- 登录成功仍然只在限流、验证码和密码全部通过后签发 Token。

### 3.2 管理员强制下线的限流档位

- [x] 将错误登记的 `force_logout_user` 改为真实 `operation_id`：`revoke_user_sessions`。
- [x] 管理员强制下线严格低级用户的全部会话使用管理变更额度，默认 `60 次/分钟`。
- [x] 建立集中、可检查的操作 ID 与限流策略映射，避免字符串改名后静默落入普通写入额度。
- [x] 增加测试，枚举所有受保护路由并确认每个路由使用预期档位。

验收标准：`revoke_user_sessions` 明确使用管理变更额度；不存在已注册路由落入非预期默认档位的情况。

### 3.3 用户管理接口可用性

- [x] 在有权查看的用户列表和详情响应中增加 `user_name`。
- [x] 用户列表增加可选的用户名精确查询参数；匹配规则必须与项目选定的用户名大小写策略一致。
- [x] 用户名过滤、严格层级过滤、软删除过滤、分页和总数统计全部在 PostgreSQL 查询中完成。
- [x] 普通管理员仍然不能通过列表、详情、查询结果、总数或错误差异发现自己、同级、更高级或受保护用户。
- [x] 不把用户名加入 JWT、运行日志、RBAC 审计快照或不需要它的公开响应。

验收标准：管理员可以用用户名找到其有权管理的低级用户，但对隐藏用户的表现与不存在用户一致；列表查询数量不会随页面大小增长。

### 3.4 在线会话响应禁止缓存

- [x] `/api/v1/me/sessions` 增加 `Cache-Control: no-store`。
- [x] 增加 HTTP 路由测试，验证统一响应、请求 ID、认证、限流、Redis 故障和禁止缓存响应头。
- [x] 响应继续只表示有效登录数量和登录时间，不声称识别物理设备。

验收标准：成功和错误响应均不会被浏览器或中间代理当成可缓存的账号安全数据。

### 3.5 就绪检查

- [x] 保留 `/health/live` 作为只表示进程存活的探针。
- [x] 新增 `/health/ready`，在短超时内验证 PostgreSQL 只有唯一且准确的
  `0004_password_auth` Alembic head，并能读取 `rbac_state(scope='global')`。
- [x] 活跃 JTI Redis 和限流 Redis 都必须通过 `PING` 与 Lua `PSETEX`、`GET`、
  `DEL` 写读删探测；使用随机非敏感 Key，正常立即删除，异常最多保留五秒。
- [x] 对外只返回就绪或不可用，不暴露连接地址、凭据、数据库名、Redis Key 或内部异常文本。
- [x] 内部结构化日志记录失败依赖和安全异常分类，并带服务器生成的 `request_id`。
- [x] 为全部依赖可用、任一依赖不可用、超时和恢复场景增加测试。

验收标准：部署系统可以区分“程序进程还活着”和“服务已经可以可靠处理认证请求”。

## 4. Skill 和文档一致性

### 4.1 默认提示

- [x] 按 `skill-creator` 的 `openai.yaml` 规范修正 `agents/openai.yaml`。
- [x] 明确五种图形验证码是新项目的固定基线。
- [x] 明确邮件验证码、短信验证码和 MFA 才是按需扩展。
- [x] 将含糊的 `recovery` 改成管理员临时密码重置和唯一超级管理员线下恢复。
- [x] 明确基础模板没有匿名“忘记密码”接口。

### 4.2 清理过期状态

- [x] 删除活跃参考文档中把当前资产称为 `Unreleased` 的表述。
- [x] 删除“登录、凭据校验或 Token 签发仍未接入”的过期说明。
- [x] 历史版本差异只保留在 Changelog 或明确标注的历史归档中。
- [x] 将旧设计记录移至 `docs/history/v0.4.0/DESIGN_REVIEW.zh-CN.md`，并在首行标明它不代表当前实现。
- [x] 修正 `rbac_state` 文档，列出 `scope`、`epoch`、`public_registration_enabled` 三个当前字段。

### 4.3 业务审计定位

- [x] 明确 `business_audit_events` 表、模型和安全写入工具属于随附基础设施。
- [x] 没有经过项目确认的动作目录时，不得为普通 CRUD 自动写业务审计事件。
- [x] 只有资金、所有权、重要状态变化、删除/恢复、人工覆盖、敏感导出或明确合规需要才启用对应事件。
- [x] 不恢复 `business_audit_delivery_outbox`、投递 Worker、租约、重试或死信设计。

### 4.4 可选模块边界

- [x] 明确国家目录和代理可用性检测只是在已经使用本 Skill 的项目中的按需扩展。
- [x] 不因用户表、代理 CRUD、地区字段或普通 FastAPI 项目自动加载这些参考。
- [x] 本版本不拆分新的 Skill；以后确有独立使用需求时再单独立项。

### 4.5 中文文档

- [x] 新增一份中文架构总览，解释认证、RBAC、限流、验证码、JWT/JTI、日志、审计、数据表和主要接口。
- [x] 中文总览服务于维护者和下载使用者；英文 reference 继续作为 AI 执行时的唯一详细规范来源。
- [x] 不机械复制 26 份 reference，避免中英文长期漂移。
- [x] README 中增加中文总览入口，并保持中英文 README 的版本、安装和限制说明一致。

## 5. 主 Skill 的 Token 优化

- [x] 将 `SKILL.md` 从当前约 465 行、34 KB 压缩到不超过约 250 行；这是减少重复内容的软目标，不能为了达到行数而删除安全底线或必要路由说明。
- [x] 主入口只保留：适用范围、检查项目、一次性项目决定、按需读取表、跨模块安全底线、实施流程和完成门槛。
- [x] JWT、验证码、限流、具体表字段、事务伪代码、详细测试矩阵只在对应 reference 保留一份。
- [x] 不创建另一个重复全部内容的“大而全”参考文件。
- [x] 保证每个 reference 都能从 `SKILL.md` 的明确任务条件找到。
- [x] 更新发布校验器，使其检查真实行为和必要约束，减少对整句固定措辞的依赖。

### 5.1 一次性项目决定清单

新项目开始实现前，只集中询问一次尚未确定的内容：

1. 用户名是否区分大小写；首尾空白仍强制删除。该项没有通用推荐值，必须由项目方明确选择。
2. 密码除 8～60 位外，是否分别强制大写、小写、数字、特殊符号；默认均不强制。
3. 一个用户最多允许多少个同时有效登录；该项没有通用推荐数字，必须由项目方提供正整数，达到上限仍默认淘汰最早登录。
4. 本次涉及接口的限流次数和窗口是否接受默认值。
5. 是否明确同意同时启用 JWT `iss` 和 `aud`；未回复仍代表不启用。
6. 生产环境是否有专业运维负责 PostgreSQL 迁移角色和运行角色分离。
7. 仅对存量项目询问没有本地密码的旧账号如何接入。

已确定的问题不得重复询问。`全部接受` 只能接受已经在同一批次中展示过的具体建议值，不能代替项目方回答用户名大小写或同时登录上限。Access Token 默认一小时仍须告知使用者及修改位置，但不是新的阻塞确认项。

## 6. 兼容性和后续迁移规则

- [x] 将官方 CI 验证环境明确写为 Python 3.12、PostgreSQL 17、Redis 7。
- [x] 其他版本可以由具体项目验证，但文档不得把未测试版本描述成已经验证。
- [x] `v0.5.1` 不修改现有数据库结构，因此不需要新增业务迁移。
- [x] 从 `v0.5.0` 开始，任何已发布表结构的后续变化必须新增向前 Alembic 迁移，不得重写已经发布的迁移文件。
- [x] 破坏性行为必须提高相应版本并在中英文 Changelog 与升级说明中醒目标注。
- [x] 可复用资产不强制附带一个适用于所有项目的依赖锁文件；生成的具体项目必须根据自己的部署环境锁定依赖。

## 7. CI 和自动校验

- [x] CI 增加 `ruff format --check`，同时保留 Ruff lint 和 strict mypy。
- [x] 发布校验器拒绝旧的 `Unreleased` 状态、错误的验证码可选描述和错误的找回密码描述。
- [x] 自动核对所有受保护路由的 `operation_id` 与限流档位。
- [x] 自动核对 `SKILL.md` 引用、重要边界和发布包内容，不用大量完整句子匹配代替行为测试。
- [x] GitHub Actions 使用完整提交 SHA 固定，并记录对应动作版本，降低供应链漂移风险。
- [x] 使用 `pip-audit` 增加依赖漏洞扫描；发现已知漏洞时 CI 失败。确实没有可用修复时，只允许通过仓库内显式记录的临时例外继续，例外必须写明漏洞、影响、原因、负责人和复查日期，不能静默忽略。
- [x] 保持发布树不包含 `.env`、真实数据库/Redis 地址、密码、Token、私钥、缓存或测试数据库文件。

## 8. 完整测试门槛

发布前必须全部通过：

- [x] `skill-creator` 快速校验。
- [x] 仓库发布包校验器。
- [x] `ruff format --check`。
- [x] Ruff lint。
- [x] strict mypy。
- [x] `pip-audit` 无未记录的已知漏洞或忽略项。
- [x] 全部非 PostgreSQL 测试。
- [ ] 全部 PostgreSQL 17 集成测试。
- [ ] 真实 Redis 7 的活跃 JTI、图形验证码、固定窗口限流和并发测试。
- [ ] Alembic 从空库升级到唯一 head、降级和再次升级测试。
- [x] 登录/注册无效验证码仍计入业务额度的回归测试。
- [x] 所有管理路由限流分类测试。
- [x] 用户名返回、搜索和严格层级防泄露测试。
- [ ] 用户和角色列表从 `page_size=1` 增加到 `page_size=200` 时 SELECT 数量保持相同，并且每次完整 HTTP 请求最多执行 8 条 SELECT；如果实现确实需要更多，必须先说明每条查询的用途并更新本计划。
- [x] `/me/sessions` 禁止缓存测试。
- [x] `/health/live` 与 `/health/ready` 的本地行为和故障测试；真实依赖成功路径仍由隔离 CI 验证。
- [ ] 日志、响应和审计不泄露密码、JWT/JTI、验证码答案、数据库/Redis URL 或代理凭据。
- [x] 独立代理前向测试：给一个未参与实现的代理固定请求“使用 `$fastapi-templates-xtn` 创建一个全新的 PostgreSQL/Redis FastAPI 用户与角色权限服务，其余产品决定尚未提供”。代理必须先一次性展示未决定事项和建议值，不得直接写代码，不得默认加入 `iss`/`aud`、Refresh Token、多租户、邮箱找回或全站限流，也不得递归读取全部 reference。
- [x] 再使用一个只涉及现有项目单项 RBAC 修改的固定请求做按需读取测试；代理只能读取入口、直接相关 reference、目标代码及对应测试，并在报告中列出实际读取的 reference。

所有 PostgreSQL 和 Redis 集成测试只允许使用新建、空白、可丢弃且明确隔离的目标，不接触现有项目或生产数据。

## 9. 明确不做的功能

`v0.5.1` 不得重新加入：

- Refresh Token。
- PostgreSQL Token 表、PostgreSQL 会话表或逐请求 Token 表查询。
- 多租户。
- 全站、全用户或跨业务共享限流额度。
- 在线超级管理员交接接口；仍使用线下受控 SQL。
- 普通用户“退出全部登录”；普通用户仍只能退出当前 Token。
- 匿名忘记密码或邮箱/短信找回。
- 默认邮件验证码、短信验证码或 MFA。
- 独立 `can_delegate` 子系统、角色继承、通配符权限或 deny 权限。
- 默认国家数据集、默认国家表或默认代理检测功能。
- `business_audit_delivery_outbox` 及其 Worker。
- 没有明确产品需求的后台任务、消息队列、设备指纹或物理设备识别。

继续保持以下约束：

- 五种固定图形验证码用途是 `login`、`register`、`admin_create`、`admin_reset`、`self_change`；每张验证码五分钟有效且只允许尝试一次。
- 每种验证码的领取和刷新使用独立额度，默认每个主体 `10 次/5 分钟`；未登录按固定业务加可信 IP，登录后按固定业务加服务端认证的用户 ID。
- 登录默认每个可信 IP `20 次/5 分钟`，注册默认每个可信 IP `5 次/小时`，完成临时密码默认每个可信 IP `20 次/5 分钟`；所有默认值集中放在 `app/settings.py`、`.env.example` 和资产 README，并在生成项目时告知使用者修改位置。
- 普通业务查询、业务修改和管理接口默认都必须登录。匿名只允许图形验证码领取/刷新、登录、公开注册、临时密码完成、只返回注册开关的状态读取，以及不返回业务数据的健康探针。
- 公开注册默认开启，但以 PostgreSQL 持久开关为准且只有 `super_admin` 可以修改；关闭公开注册不影响已有用户登录和有权限的管理员新增用户。
- 公开注册和管理员新增用户都只能在同一事务中绑定最低级 `user` 角色，不能接受客户端指定初始角色。
- 首位 `super_admin` 只能由开发或运维人员使用目标用户不可变 ID 执行受保护的线下 PostgreSQL 脚本绑定，不能增加在线初始化接口或自动提升首位注册用户。
- 管理员设置的临时密码不按时间过期，但只能用于一次正式密码设置；成功完成、再次重置或账号删除后原临时密码立即失效。
- 用户名必须去除首尾空白并满足 `3～32` 位 ASCII 英文字母、数字或下划线；继续禁止 `admin`、`administrator`、`root`、`superadmin`、`super_admin`、`sysadmin`、`system`、`support`、`user`、`test`、`guest`、`ceshi` 完整名称且不区分大小写。
- 用户软删除后原用户名永久保留，不能重新分配给其他账号。
- JWT 默认只有 `sub`、`jti`、`iat`、`exp`、`token_type`。
- `iss` 和 `aud` 只能在使用者明确同意后成对加入。
- Access Token 默认一小时，使用时提醒项目使用者按业务调整。
- 必须经过 Redis 活跃 JTI 校验，不增加 Refresh Token。
- `super_admin`、`admin`、`user` 是不可删除的系统角色。
- 每个用户最多 10 个未解绑角色。
- 普通数据删除使用软删除；审计事件只追加；固定状态行不删除。
- 所有管理读取和写入继续执行严格层级、隐藏目标和禁止自我提权规则。

## 10. 交付和发布

- [x] 更新资产版本为 `0.5.1`。
- [x] 更新中英文 README、Changelog、发布清单和相关参考文档。
- [x] 完成代码审查、文档审查和独立前向测试。
- [x] 确认仓库工作树只包含本计划范围内的修改。
- [x] 同步仓库 Skill 到当前维护者的 `$CODEX_HOME/skills/fastapi-templates-xtn`，并校验全部受版本控制文件哈希一致。
- [ ] 提交并推送 `main`，等待准确提交的 GitHub CI 全部成功。
- [ ] 在同一提交创建不可变 `v0.5.1` 标签和正式 GitHub Release。
- [ ] 验证 Release 不是草稿或预发布，源码 ZIP 可以下载，标签安装路径可用。

## 11. 偏离控制

实施过程中如果出现以下情况，必须暂停相关修改并先向 XTN 汇报：

- 需要新增本文未列出的数据表、外部服务或后台 Worker。
- 需要改变已经确认的认证、验证码、限流、JWT、角色层级或软删除行为。
- 修复会改变公开 API 路径、请求字段或现有成功响应结构。
- 需要修改已经发布的 `v0.5.0` 标签或重写其迁移历史。
- 测试只能通过降低安全要求、跳过关键集成测试或使用真实业务数据库。

汇报必须说明：发现的问题、为什么原计划无法继续、建议方案、风险、是否存在更简单的替代方案。未取得明确同意前，不扩大范围。
