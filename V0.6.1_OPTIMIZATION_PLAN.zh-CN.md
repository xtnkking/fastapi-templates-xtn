# fastapi-templates-xtn v0.6.1 优化计划

状态：实施与验证中，禁止发布
制定日期：2026-09-20
目标版本：`v0.6.1`

## 目标和边界

本文记录 XTN 已确认的 12 项修复及发布前发现的回归问题。修复不得加入多租户、
Refresh Token、PostgreSQL Token／会话表、全站限流、匿名找回密码或在线超级管理员
交接。`RELEASE_CHECKLIST.zh-CN.md` 保持 `DRAFT`，直到准确候选提交的全部证据完成。

## 已确认的 12 项

- [x] 匿名登录、注册验证码不绑定领取时 IP；仍绑定场景、五分钟有效且成功失败都只
  消费一次。领取和提交分别按各自请求的可信 IP 做业务限流。
- [x] 运行文档列出互不相同的必填 `JWT_SECRET` 与 `RATE_LIMIT_HMAC_KEY`，且禁止
  使用仓库示例值或把真实值提交到版本库。
- [x] 已登录接口在 JWT 与 Redis 活跃 JTI 通过后，先只读检查现有的“业务 + 用户”
  窗口；额度未满才访问 PostgreSQL，只有数据库确认身份仍有效后才正式扣除额度。
  数据库拒绝的旧 JTI 尽力从 Redis 删除，删除失败仍保持原 `401`。
- [x] 已用当前维护源安全更新本机安装目录；旧安装完整保留在本机带时间戳的
  `skill-backups` 目录，并逐文件 SHA-256 核对，避免再次产生混合版本。
- [x] 增加可重复执行的安装／更新工具：暂存复制、清单核对、旧版本备份、失败回滚、
  路径与链接防护；相同版本且清单相同时不重复替换或备份。
- [x] `skills/fastapi-templates-xtn/VERSION` 是唯一版本来源，资产版本、文档、校验器和
  安装后版本显示均由它核对。
- [x] 多语言 reference 直接列出实现文件、`zh-CN`／`en` 资源、路由接入点和测试入口。
- [x] 主 `SKILL.md` 改为按任务读取，详细实现只保留在对应 reference；资产代码按符号
  和对应测试检索，不整目录加载。
- [x] 自动触发范围收窄：只有明确点名本 Skill 或采用这一整套 PostgreSQL、Redis、
  本地密码和固定层级权限架构时才适用，不接管普通 FastAPI／RBAC 任务。
- [x] JWT 默认不含 `iss`、`aud`；只有使用者明确同意后才成对加入，存量项目已成对
  配置时默认保留，禁止只配置其中一个。
- [x] CI 固定 CPython 3.12/Linux 的精确解析，递归检查 runtime/test 完整依赖闭包、
  extras 和 markers，并执行 `pip check` 与 `pip-audit`；`uvicorn[standard]` 的
  `uvloop` 必须在锁中。
- [x] wheel 校验覆盖运行时代码、两份语言资源和三份法律声明；发布就绪校验必须读取
  真实 `READY` 状态和逐项证据，不能把计划标题当成已经发布。

## 发布前新增回归检查

- [x] 根据 XTN 的最终选择，将 Access Token 默认有效期从 3600 秒调整为 86400 秒
  （24 小时）；配置、示例、测试和当前文档保持一致，并明确高风险后台应缩短有效期。
- [x] 私有验证码入口的只读限流预检与正式路由复用同一个 Content-Type、JSON 和
  Schema 分类函数。`text/plain` 即使包含合法 JSON 也只能进入拒绝场景额度；额度已满
  时不得访问 PostgreSQL。
- [x] 运行全部非 PostgreSQL 测试、Ruff format/lint、strict mypy、Skill 快速校验、
  发布校验、更新器测试、wheel 构建与内容校验、精确锁依赖审计。
- [ ] 在可丢弃的 PostgreSQL 17 与 Redis 7 环境运行集成、迁移与并发测试；如果本轮
  环境不可用，必须明确写入发布阻塞项，不能声称已验证或将清单改为 `READY`。
- [x] 完成最终差异审查后同步本机 Skill；再次 dry-run 显示相同版本和相同清单，
  不产生第二份备份。新任务或重启 Codex 后才验证新 Skill 已重新加载。

## 当前验证证据

- 聚焦 CAPTCHA／身份／限流顺序测试：`73 passed`。
- 全部非 PostgreSQL 测试：`579 passed, 1 skipped, 155 deselected`；跳过项是需要明确
  确认的空白 Redis 实例。
- 独立真实 Redis 7 测试：`1 passed`，覆盖验证码原子消费、刷新竞争、活跃 JTI、
  同时登录淘汰和限流；所用空白逻辑库测试后已清理为零 Key。
- Ruff format/lint、strict mypy（93 个源文件）、Skill 快速校验、更新器 13 项测试、
  CI 依赖校验器 5 项测试、国家校验器 18 项测试、wheel 构建与内容校验、开发态
  发布校验（179 个文件）均通过。
- 精确锁文件的离线漏洞审计结果为无已知漏洞；完整依赖闭包脚本只能在目标
  CPython 3.12/Linux 环境执行，本机 Python 3.13/Windows 会按设计拒绝。
- 当前可访问 PostgreSQL 实际为 16，且账号不能创建全新临时数据库，因此没有在现有
  业务库执行迁移、清表或并发测试。最终发布仍需要 GitHub CI 的 PostgreSQL 17、
  Redis 7 和 CPython 3.12/Linux 证据。
- 本机同步后的只读 dry-run 退出码为 `0`：源与安装目录版本均为 `0.6.1`，双方
  SHA-256 清单均含 148 个文件且完全一致；未改动文件，也未创建第二份备份。

## 发布停止条件

任何测试失败、依赖闭包未锁定、本机清单不一致、真实 PostgreSQL／Redis 证据缺失，
或发布清单仍为 `DRAFT` 时，都不得创建或移动 `v0.6.1` 标签，也不得发布 Release。
