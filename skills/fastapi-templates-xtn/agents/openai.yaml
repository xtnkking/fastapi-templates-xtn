interface:
  display_name: "FastAPI Templates XTN"
  short_description: "FastAPI RBAC, local auth, tokens, and audits"
  default_prompt: "Use $fastapi-templates-xtn to build a PostgreSQL FastAPI service with strict RBAC, complete Argon2id username/password authentication and recovery, Redis-gated Access Tokens, configurable rate limits, optional verification when explicitly requested, and durable audits."
